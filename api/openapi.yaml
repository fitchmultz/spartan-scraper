---
openapi: 3.0.3
info:
  title: Spartan Scraper API
  version: 1.0.0-rc1
servers:
- url: http://localhost:8741
security:
- ApiKeyAuth: []
paths:
  "/healthz":
    get:
      summary: Health check
      security: []
      responses:
        '200':
          description: Structured health, setup, and runtime diagnostics.
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/HealthResponse"
  "/v1/diagnostics/browser-check":
    post:
      summary: Re-check browser tooling availability
      operationId: postV1DiagnosticsBrowserCheck
      responses:
        '200':
          description: Browser diagnostic result
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DiagnosticActionResponse"
  "/v1/diagnostics/ai-check":
    post:
      summary: Re-check AI prerequisites
      operationId: postV1DiagnosticsAiCheck
      responses:
        '200':
          description: AI diagnostic result
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DiagnosticActionResponse"
  "/v1/diagnostics/proxy-pool-check":
    post:
      summary: Re-check proxy-pool configuration
      operationId: postV1DiagnosticsProxyPoolCheck
      responses:
        '200':
          description: Proxy-pool diagnostic result
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DiagnosticActionResponse"
  "/v1/auth/profiles":
    get:
      summary: List auth profiles
      responses:
        '200':
          description: Auth profiles
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AuthProfilesResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/profiles/{name}":
    put:
      summary: Upsert auth profile
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AuthProfile"
      responses:
        '200':
          description: Auth profile saved
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AuthProfile"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete auth profile
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/import":
    post:
      summary: Import auth vault
      description: |
        Import auth vault from a file in the data directory.
        The path must be a simple filename (no directory separators or path traversal).
        The file is resolved relative to the configured DATA_DIR.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AuthVaultImport"
      responses:
        '200':
          description: Imported
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/export":
    post:
      summary: Export auth vault
      description: |
        Export auth vault to a file in the data directory.
        The path must be a simple filename (no directory separators or path traversal).
        The file is resolved relative to the configured DATA_DIR.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AuthVaultExport"
      responses:
        '200':
          description: Exported
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/oauth/initiate":
    post:
      summary: Initiate OAuth 2.0 authorization flow
      description: |
        Starts an OAuth 2.0 authorization code flow. Generates state parameter for CSRF protection
        and optionally PKCE code challenge. Returns the authorization URL to redirect the user to.
      operationId: initiateOAuth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/OAuthInitiateRequest"
      responses:
        '200':
          description: Authorization URL generated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OAuthInitiateResponse"
        '400':
          description: Bad Request - Missing profile or invalid OAuth configuration
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Profile not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/oauth/callback":
    get:
      summary: OAuth 2.0 callback endpoint
      description: |
        Handles the OAuth 2.0 callback from the authorization server. Exchanges the authorization
        code for access and refresh tokens. The state parameter is validated for CSRF protection.
      operationId: oauthCallback
      parameters:
      - name: code
        in: query
        description: Authorization code from OAuth provider
        schema:
          type: string
      - name: state
        in: query
        required: true
        description: State parameter for CSRF validation
        schema:
          type: string
      - name: error
        in: query
        description: Error from OAuth provider (if authentication failed)
        schema:
          type: string
      responses:
        '200':
          description: Token exchange successful
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OAuthTokenResponse"
        '400':
          description: Bad Request - Invalid state or missing code
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/oauth/refresh":
    post:
      summary: Refresh OAuth 2.0 access token
      description: |
        Refreshes an OAuth 2.0 access token using the stored refresh token.
        The new tokens are automatically saved for the profile.
      operationId: refreshOAuthToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/OAuthRefreshRequest"
      responses:
        '200':
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OAuthTokenResponse"
        '400':
          description: Bad Request - Missing profile or no refresh token
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Profile or token not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/oauth/discover":
    post:
      summary: Perform OIDC discovery
      description: |
        Fetches OIDC provider metadata from a discovery URL or issuer.
        Returns endpoints, supported scopes, and other provider configuration.
      operationId: discoverOIDC
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/OIDCDiscoverRequest"
      responses:
        '200':
          description: Discovery successful
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/OIDCProviderMetadata"
        '400':
          description: Bad Request - Missing discovery URL or issuer
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/auth/oauth/revoke":
    post:
      summary: Revoke OAuth 2.0 token
      description: |
        Revokes the OAuth 2.0 token for a profile at the provider (if supported)
        and deletes it from local storage.
      operationId: revokeOAuthToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - profile_name
              properties:
                profile_name:
                  type: string
                  description: Name of the profile to revoke tokens for
      responses:
        '200':
          description: Token revoked successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request - Missing profile name
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Profile or token not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/templates":
    get:
      summary: List extraction templates
      description: Returns list of all available extraction templates (built-in +
        file-based)
      operationId: listTemplates
      responses:
        '200':
          description: Template list
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      type: string
                    description: List of template names
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create a new template
      description: Create a custom extraction template
      operationId: createTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/CreateTemplateRequest"
      responses:
        '201':
          description: Template created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TemplateDetail"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/templates/{name}":
    get:
      summary: Get template details
      description: Returns full details of a specific template
      operationId: getTemplate
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Template details
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TemplateDetail"
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    put:
      summary: Update template
      description: Update an existing custom template
      operationId: updateTemplate
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/CreateTemplateRequest"
      responses:
        '200':
          description: Template updated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TemplateDetail"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '403':
          description: Forbidden - cannot modify built-in template
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete custom template
      description: Delete a custom template (built-in templates cannot be deleted)
      operationId: deleteTemplate
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Deleted
        '403':
          description: Forbidden - cannot delete built-in template
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Template not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/template-preview":
    get:
      summary: Fetch page DOM structure for selector building
      description: Fetches a remote page and returns its simplified DOM tree for visual
        selector building
      operationId: getTemplatePreview
      parameters:
      - name: url
        in: query
        required: true
        schema:
          type: string
          format: uri
        description: URL to fetch and analyze
      - name: headless
        in: query
        schema:
          type: boolean
          default: false
        description: Use headless browser for fetching
      - name: playwright
        in: query
        schema:
          type: boolean
          default: false
        description: Use Playwright instead of Chromedp for headless fetching
      responses:
        '200':
          description: DOM preview
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TemplatePreviewResponse"
        '400':
          description: Bad Request - invalid URL
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/template-preview/test-selector":
    post:
      summary: Test CSS selector against URL
      description: Tests a CSS selector against a fetched page and returns matching
        elements
      operationId: testSelector
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/TestSelectorRequest"
      responses:
        '200':
          description: Selector test results
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TestSelectorResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/extract-preview":
    post:
      summary: Preview AI-powered extraction
      description: Extract structured data using AI/LLM without creating a job.
        Provide HTML directly or let Spartan fetch the URL through its existing
        fetch stack.
      operationId: aiExtractPreview
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIExtractPreviewRequest"
      responses:
        '200':
          description: AI extraction preview results
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIExtractPreviewResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/template-generate":
    post:
      summary: Generate extraction template using AI
      description: Generate an extraction template using AI from either fetched page content
        or raw HTML supplied directly.
      operationId: aiTemplateGenerate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIExtractTemplateGenerateRequest"
      responses:
        '200':
          description: Generated template
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIExtractTemplateGenerateResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/template-debug":
    post:
      summary: Debug extraction template using AI
      description: Analyze an existing extraction template against fetched page content or raw HTML,
        then optionally suggest a repaired template.
      operationId: aiTemplateDebug
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIExtractTemplateDebugRequest"
      responses:
        '200':
          description: Template debug diagnostics and repair suggestions
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIExtractTemplateDebugResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/render-profile-generate":
    post:
      summary: Generate render profile using AI
      description: Generate a Spartan render profile for a live page without creating a job.
      operationId: aiRenderProfileGenerate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIRenderProfileGenerateRequest"
      responses:
        '200':
          description: Generated render profile
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIRenderProfileGenerateResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/render-profile-debug":
    post:
      summary: Debug render profile using AI
      description: Recheck an existing render profile against a live page, report deterministic issues,
        and optionally suggest a tuned replacement.
      operationId: aiRenderProfileDebug
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIRenderProfileDebugRequest"
      responses:
        '200':
          description: Render profile debug diagnostics and tuned suggestion
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIRenderProfileDebugResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/pipeline-js-generate":
    post:
      summary: Generate pipeline JS script using AI
      description: Generate a Spartan pipeline JS script for a live page without creating a job.
      operationId: aiPipelineJsGenerate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIPipelineJSGenerateRequest"
      responses:
        '200':
          description: Generated pipeline JS script
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIPipelineJSGenerateResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/pipeline-js-debug":
    post:
      summary: Debug pipeline JS script using AI
      description: Recheck an existing pipeline JS script against a live page, report deterministic
        issues, and optionally suggest a tuned replacement.
      operationId: aiPipelineJsDebug
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIPipelineJSDebugRequest"
      responses:
        '200':
          description: Pipeline JS debug diagnostics and tuned suggestion
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIPipelineJSDebugResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/research-refine":
    post:
      summary: Refine bounded research output using AI
      description: Rewrite, condense, and normalize an existing research result without creating a job or launching new browsing.
      operationId: aiResearchRefine
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIResearchRefineRequest"
      responses:
        '200':
          description: Refined research brief
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIResearchRefineResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/export-shape":
    post:
      summary: Generate or tune bounded export shaping using AI
      description: Propose deterministic export-ready field selections, labels, and formatting hints from a representative job result without creating a job.
      operationId: aiExportShape
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AIExportShapeRequest"
      responses:
        '200':
          description: Generated export shape
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AIExportShapeResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ai/transform-generate":
    post:
      summary: Generate or tune bounded result transforms using AI
      description: Propose a deterministic JMESPath or JSONata transform from a representative saved job result without creating a job.
      operationId: aiTransformGenerate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/AITransformGenerateRequest"
      responses:
        '200':
          description: Generated result transform
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/AITransformGenerateResponse"
        '400':
          description: Bad Request - invalid parameters
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/crawl-states":
    get:
      summary: List crawl states
      description: Returns list of crawl states used for incremental tracking, ordered
        by last_scraped DESC
      operationId: listCrawlStates
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 1000
        description: Maximum number of crawl states to return
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of crawl states to skip
      responses:
        '200':
          description: Crawl states list
          headers:
            X-Total-Count:
              description: Total number of crawl states available
              schema:
                type: integer
          content:
            application/json:
              schema:
                type: object
                properties:
                  crawlStates:
                    type: array
                    items:
                      "$ref": "#/components/schemas/CrawlState"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Clear or delete crawl states
      description: |
        Deletes a specific crawl state if 'url' parameter is provided.
        Clears all crawl states if no 'url' parameter is provided.
      operationId: deleteCrawlStates
      parameters:
      - in: query
        name: url
        schema:
          type: string
        description: URL of the crawl state to delete. If omitted, all states are
          cleared.
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/dedup/duplicates":
    get:
      summary: Find duplicate content
      description: Find URLs with similar content across all jobs using simhash
      operationId: findDuplicates
      parameters:
      - in: query
        name: simhash
        required: true
        schema:
          type: integer
          format: uint64
        description: The simhash value to find duplicates for
      - in: query
        name: threshold
        schema:
          type: integer
          default: 3
          minimum: 0
          maximum: 64
        description: Hamming distance threshold (0=exact match, 3=near-duplicates,
          8=similar)
      responses:
        '200':
          description: List of duplicate matches
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/DuplicateMatch"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/dedup/history":
    get:
      summary: Get content history for URL
      description: Returns all indexed entries for a URL across jobs
      operationId: getContentHistory
      parameters:
      - in: query
        name: url
        required: true
        schema:
          type: string
        description: The URL to get content history for
      responses:
        '200':
          description: List of content entries
          content:
            application/json:
              schema:
                type: array
                items:
                  "$ref": "#/components/schemas/ContentEntry"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/dedup/stats":
    get:
      summary: Get deduplication statistics
      description: Returns statistics about indexed content and duplicates
      operationId: getDedupStats
      responses:
        '200':
          description: Deduplication statistics
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/DedupStats"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/dedup/job/{jobId}":
    delete:
      summary: Remove dedup entries for a job
      description: Deletes all content fingerprints associated with a job
      operationId: deleteJobDedupEntries
      parameters:
      - in: path
        name: jobId
        required: true
        schema:
          type: string
        description: The job ID to remove entries for
      responses:
        '204':
          description: Entries deleted successfully
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/retention/status":
    get:
      summary: Get retention status
      description: Returns current retention configuration and statistics
      operationId: getRetentionStatus
      tags:
      - Retention
      responses:
        '200':
          description: Retention status
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RetentionStatusResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/proxy-pool/status":
    get:
      summary: Get proxy pool status
      description: Returns the currently loaded proxy pool strategy, aggregate counts, available regions/tags, and per-proxy stats.
      operationId: getProxyPoolStatus
      tags:
      - ProxyPool
      responses:
        '200':
          description: Proxy pool status
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ProxyPoolStatusResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/retention/cleanup":
    post:
      summary: Run retention cleanup
      description: Execute retention cleanup with optional dry-run mode
      operationId: runRetentionCleanup
      tags:
      - Retention
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RetentionCleanupRequest"
      responses:
        '200':
          description: Cleanup completed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RetentionCleanupResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/scrape":
    post:
      summary: Scrape a single page
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ScrapeRequest"
      responses:
        '201':
          description: Job created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '503':
          description: Service Unavailable
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/crawl":
    post:
      summary: Crawl a site
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/CrawlRequest"
      responses:
        '201':
          description: Job created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '503':
          description: Service Unavailable
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/research":
    post:
      summary: Deep research across multiple sources
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ResearchRequest"
      responses:
        '201':
          description: Job created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '503':
          description: Service Unavailable
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs":
    get:
      summary: List jobs
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 1000
        description: Maximum number of jobs to return
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of jobs to skip
      - in: query
        name: status
        schema:
          type: string
          enum:
          - queued
          - running
          - succeeded
          - failed
          - canceled
        description: Filter jobs by status (omitted = all jobs)
      responses:
        '200':
          description: Job list
          headers:
            X-Total-Count:
              description: Total number of jobs matching the filter
              schema:
                type: integer
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobList"
        '400':
          description: Bad Request (invalid status parameter)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/failures":
    get:
      summary: List failed jobs
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          default: 50
          minimum: 1
          maximum: 1000
        description: Maximum number of failed jobs to return
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of failed jobs to skip
      responses:
        '200':
          description: Failed job list
          headers:
            X-Total-Count:
              description: Total number of failed jobs
              schema:
                type: integer
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobList"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/{id}":
    get:
      summary: Get a job
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Job envelope
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Cancel or delete a job
      description: |
        Cancels a queued or running job.
        With ?force=true, permanently deletes the job from the store.
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: query
        name: force
        schema:
          type: boolean
          default: false
        description: If true, permanently delete the job. If false, cancel active
          jobs.
      responses:
        '200':
          description: Cancel returned the updated job envelope, or deleted status for force=true
          content:
            application/json:
              schema:
                oneOf:
                - "$ref": "#/components/schemas/JobResponse"
                - "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/{id}/results":
    get:
      summary: Get job results
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: query
        name: format
        schema:
          type: string
          enum:
          - jsonl
          - json
          default: jsonl
        description: Output format for raw persisted results. Use jsonl for the stored
          newline-delimited artifact, or json for a JSON-encoded view.
      - in: query
        name: limit
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 1000
        description: Maximum number of result items to return (applies only to jsonl
          format)
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of result items to skip before returning results (applies
          only to jsonl format)
      responses:
        '200':
          description: Raw persisted job results
          headers:
            X-Total-Count:
              description: Total number of items available in the result set (for
                jsonl format with pagination)
              schema:
                type: integer
          content:
            application/x-ndjson:
              schema:
                type: string
            application/json:
              schema:
                type: object
        '400':
          description: Bad Request (invalid format)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/{id}/export":
    post:
      summary: Export saved job results
      description: Run a direct export, persist an export outcome record, and return guided inspection with inline artifact content.
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ResultExportRequest"
      responses:
        '200':
          description: Export outcome
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportOutcomeResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/{id}/exports":
    get:
      summary: List persisted export outcomes for a job
      description: Returns guided export outcome inspection records for a specific job.
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: query
        name: limit
        schema:
          type: integer
          default: 50
          minimum: 1
          maximum: 1000
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
      responses:
        '200':
          description: Export outcome history for the job
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportOutcomeListResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/exports/{id}":
    get:
      summary: Get a persisted export outcome
      description: Returns a single guided export outcome record by export id.
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Export outcome
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportOutcomeResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/{id}/preview-transform":
    post:
      summary: Preview transformation on job results
      description: Apply JMESPath or JSONata expression to job results and return
        preview
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/TransformPreviewRequest"
      responses:
        '200':
          description: Transformed preview results
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TransformPreviewResponse"
        '400':
          description: Bad Request (invalid expression or language)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found (job not found or no results)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/transform/validate":
    post:
      summary: Validate transformation expression
      description: Validate a JMESPath or JSONata expression without executing it
        on data
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/TransformValidateRequest"
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/TransformValidateResponse"
        '400':
          description: Bad Request (invalid request body)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/batch":
    get:
      summary: List batches
      description: Retrieve paginated batch summaries with aggregate job statistics
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 1000
        description: Maximum number of batches to return
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of batches to skip
      responses:
        '200':
          description: Batch summaries retrieved successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchListResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/batch/scrape":
    post:
      summary: Create a batch of scrape jobs
      description: Submit multiple URLs to be scraped in a single batch operation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BatchScrapeRequest"
      responses:
        '201':
          description: Batch created successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchResponse"
        '400':
          description: Bad Request (invalid batch size or URLs)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/batch/crawl":
    post:
      summary: Create a batch of crawl jobs
      description: Submit multiple URLs to be crawled in a single batch operation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BatchCrawlRequest"
      responses:
        '201':
          description: Batch created successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchResponse"
        '400':
          description: Bad Request (invalid batch size or URLs)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/batch/research":
    post:
      summary: Create a batch of research jobs
      description: Submit multiple URLs for research queries in a single batch operation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BatchResearchRequest"
      responses:
        '201':
          description: Batch created successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchResponse"
        '400':
          description: Bad Request (invalid batch size or URLs)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/jobs/batch/{id}":
    get:
      summary: Get batch status
      description: Retrieve the status of a batch including aggregated job statistics
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Batch ID
      - in: query
        name: include_jobs
        schema:
          type: boolean
          default: false
        description: Include individual job details in the response
      - in: query
        name: limit
        schema:
          type: integer
          default: 50
          minimum: 1
          maximum: 1000
        description: Maximum number of jobs to return (when include_jobs=true)
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of jobs to skip (when include_jobs=true)
      responses:
        '200':
          description: Batch envelope retrieved successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Batch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Cancel a batch
      description: Cancel all non-terminal jobs in a batch
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Batch ID
      responses:
        '200':
          description: Batch canceled successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/BatchResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Batch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/schedules":
    get:
      summary: List schedules
      responses:
        '200':
          description: Schedule list
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ScheduleListResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Add a schedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ScheduleRequest"
      responses:
        '200':
          description: Schedule created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Schedule"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/schedules/{id}":
    delete:
      summary: Delete a schedule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/export-schedules":
    get:
      summary: List export schedules
      description: Returns all automated export schedules
      operationId: listExportSchedules
      responses:
        '200':
          description: Export schedule list
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportScheduleListResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create an export schedule
      description: Creates a new automated export schedule
      operationId: createExportSchedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ExportScheduleRequest"
      responses:
        '200':
          description: Export schedule created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportSchedule"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/export-schedules/{id}":
    get:
      summary: Get an export schedule
      description: Returns a single export schedule by ID
      operationId: getExportSchedule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Export schedule
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportSchedule"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    put:
      summary: Update an export schedule
      description: Updates an existing export schedule
      operationId: updateExportSchedule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ExportScheduleRequest"
      responses:
        '200':
          description: Export schedule updated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportSchedule"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete an export schedule
      description: Deletes an export schedule by ID
      operationId: deleteExportSchedule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Deleted
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/export-schedules/{id}/history":
    get:
      summary: Get export schedule history
      description: Returns guided export outcome inspection records for a specific schedule.
      operationId: getExportScheduleHistory
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: query
        name: limit
        schema:
          type: integer
          default: 50
          minimum: 1
          maximum: 1000
      - in: query
        name: offset
        schema:
          type: integer
          default: 0
          minimum: 0
      responses:
        '200':
          description: Export outcome history
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ExportOutcomeListResponse"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch":
    get:
      summary: List all watches
      operationId: listWatches
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          maximum: 1000
          default: 100
        description: Maximum number of watches to return.
      - in: query
        name: offset
        schema:
          type: integer
          minimum: 0
          default: 0
        description: Number of watches to skip before returning results.
      responses:
        '200':
          description: List of watches
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WatchList"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create a new watch
      operationId: createWatch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/WatchInput"
      responses:
        '201':
          description: Watch created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Watch"
        '400':
          description: Bad Request (validation error)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}":
    get:
      summary: Get a watch by ID
      operationId: getWatch
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      responses:
        '200':
          description: Watch details
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Watch"
        '404':
          description: Watch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    put:
      summary: Update a watch
      operationId: updateWatch
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/WatchInput"
      responses:
        '200':
          description: Watch updated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/Watch"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Watch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a watch
      operationId: deleteWatch
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      responses:
        '204':
          description: Watch deleted
        '404':
          description: Watch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}/check":
    post:
      summary: Manually check a watch for changes
      operationId: checkWatch
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      responses:
        '200':
          description: Check completed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WatchCheckInspectionResponse"
        '404':
          description: Watch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}/history":
    get:
      summary: List persisted watch check history for a watch
      operationId: getWatchHistory
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      - in: query
        name: limit
        schema:
          type: integer
          minimum: 1
          default: 10
      - in: query
        name: offset
        schema:
          type: integer
          minimum: 0
          default: 0
      responses:
        '200':
          description: Paginated watch history
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WatchCheckHistoryResponse"
        '404':
          description: Watch not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}/history/{checkId}":
    get:
      summary: Get one persisted watch check inspection
      operationId: getWatchCheck
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      - in: path
        name: checkId
        required: true
        schema:
          type: string
        description: Watch check ID
      responses:
        '200':
          description: Watch check inspection
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WatchCheckInspectionResponse"
        '404':
          description: Watch or check not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}/history/{checkId}/artifacts/{artifactKind}":
    get:
      summary: Download a persisted watch history artifact snapshot
      operationId: getWatchCheckArtifact
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      - in: path
        name: checkId
        required: true
        schema:
          type: string
        description: Watch check ID
      - in: path
        name: artifactKind
        required: true
        schema:
          type: string
          enum:
          - current-screenshot
          - previous-screenshot
          - visual-diff
        description: Persisted watch artifact kind
      responses:
        '200':
          description: Artifact bytes
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid artifact kind
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Watch, check, or artifact not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/watch/{id}/artifacts/{artifactKind}":
    get:
      summary: Download a persisted watch artifact
      operationId: getWatchArtifact
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Watch ID
      - in: path
        name: artifactKind
        required: true
        schema:
          type: string
          enum:
          - current-screenshot
          - previous-screenshot
          - visual-diff
        description: Persisted watch artifact kind
      responses:
        '200':
          description: Artifact bytes
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid artifact kind
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Watch or artifact not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/metrics":
    get:
      summary: Get performance metrics
      description: |
        Returns current performance metrics including request rates, success rates,
        response times, fetcher usage breakdown, and rate limiter status.
      operationId: getMetrics
      responses:
        '200':
          description: Current metrics
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/MetricsResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/ws":
    get:
      summary: WebSocket endpoint for real-time job updates
      description: |
        Establishes a WebSocket connection for receiving real-time job status updates.

        ## Connection
        Upgrade HTTP connection to WebSocket protocol at `ws://host:port/v1/ws`.

        ## Message Format
        All messages are JSON with the structure:
        ```json
        {
          "type": "job_status_changed",
          "timestamp": 1706563200000,
          "payload": { ... }
        }
        ```

        ## Server -> Client Messages

        ### job_created
        Sent when a new job is enqueued.
        Payload: JobEventPayload

        ### job_started
        Sent when a job begins execution.
        Payload: JobEventPayload

        ### job_status_changed
        Sent when a job transitions between states.
        Payload: JobEventPayload

        ### job_completed
        Sent when a job reaches a terminal state (succeeded, failed, canceled).
        Payload: JobEventPayload

        ### manager_status
        Sent periodically with queue depth and active job count.
        Payload: ManagerStatusPayload

        ### metrics
        Sent every 5 seconds with performance metrics.
        Payload: MetricsResponse

        ### ping
        Heartbeat ping sent every 30 seconds.
        Client should respond with a pong message.

        ## Client -> Server Messages

        ### pong
        Response to server ping for connection health.
        ```json
        {"type": "pong", "timestamp": 1706563200000, "payload": null}
        ```

        ### subscribe_jobs
        Subscribe to job lifecycle events.
        ```json
        {"type": "subscribe_jobs", "timestamp": 1706563200000, "payload": null}
        ```

        ### unsubscribe_jobs
        Unsubscribe from job lifecycle events.
        ```json
        {"type": "unsubscribe_jobs", "timestamp": 1706563200000, "payload": null}
        ```

        ## Fallback Behavior
        If the WebSocket connection fails, clients should fall back to
        polling the `/v1/jobs` endpoint every 4 seconds.
      responses:
        '101':
          description: Switching Protocols to WebSocket
        '400':
          description: Bad Request (invalid upgrade request)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/chains":
    get:
      summary: List all job chains
      description: Returns all defined job chains ordered by creation date (newest
        first)
      operationId: listChains
      responses:
        '200':
          description: List of chains
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ChainListResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create a new job chain
      description: Creates a named, reusable workflow definition
      operationId: createChain
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ChainCreateRequest"
      responses:
        '201':
          description: Chain created successfully
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobChain"
        '400':
          description: Bad Request (invalid chain definition)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/chains/{id}":
    get:
      summary: Get a chain by ID
      description: Retrieve a specific chain definition
      operationId: getChain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Chain ID
      responses:
        '200':
          description: Chain details
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JobChain"
        '404':
          description: Chain not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a chain
      description: Removes a chain definition. Only chains with no active jobs can
        be deleted.
      operationId: deleteChain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Chain ID
      responses:
        '204':
          description: Chain deleted successfully
        '400':
          description: Bad Request (chain has active jobs)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Chain not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/chains/{id}/submit":
    post:
      summary: Submit/instantiate a chain
      description: Creates jobs from a chain definition with optional per-node request overrides
      operationId: submitChain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
        description: Chain ID
      requestBody:
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ChainSubmitRequest"
      responses:
        '201':
          description: Chain submitted, jobs created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ChainSubmitResponse"
        '400':
          description: Bad Request (invalid overrides)
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Chain not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '405':
          description: Method Not Allowed
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '415':
          description: Unsupported Media Type
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/webhooks/deliveries":
    get:
      summary: List webhook delivery records
      description: Get a list of webhook delivery records with optional filtering
        by job ID
      tags:
      - Webhooks
      parameters:
      - name: job_id
        in: query
        schema:
          type: string
        description: Filter deliveries by job ID
      - name: limit
        in: query
        schema:
          type: integer
          default: 100
          minimum: 1
          maximum: 1000
        description: Maximum number of records to return
      - name: offset
        in: query
        schema:
          type: integer
          default: 0
          minimum: 0
        description: Number of records to skip
      responses:
        '200':
          description: List of webhook delivery records
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WebhookDeliveryListResponse"
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/webhooks/deliveries/{id}":
    get:
      summary: Get a single webhook delivery record
      description: Get detailed information about a specific webhook delivery
      tags:
      - Webhooks
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
        description: Delivery record ID
      responses:
        '200':
          description: Webhook delivery record
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/WebhookDeliveryRecord"
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/render-profiles":
    get:
      summary: List render profiles
      description: Get all configured render profiles
      tags:
      - RenderProfiles
      responses:
        '200':
          description: List of render profiles
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RenderProfilesResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create render profile
      description: Create a new render profile
      tags:
      - RenderProfiles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RenderProfileInput"
      responses:
        '201':
          description: Profile created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RenderProfile"
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '409':
          description: Profile already exists
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/render-profiles/{name}":
    get:
      summary: Get render profile
      description: Get a specific render profile by name
      tags:
      - RenderProfiles
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Render profile
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RenderProfile"
        '404':
          description: Profile not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    put:
      summary: Update render profile
      description: Update an existing render profile
      tags:
      - RenderProfiles
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/RenderProfileInput"
      responses:
        '200':
          description: Profile updated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/RenderProfile"
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Profile not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete render profile
      description: Delete a render profile
      tags:
      - RenderProfiles
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Profile deleted
        '404':
          description: Profile not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/pipeline-js":
    get:
      summary: List pipeline JS scripts
      description: Get all configured pipeline JavaScript scripts
      tags:
      - PipelineJS
      responses:
        '200':
          description: List of pipeline JS scripts
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PipelineJSResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    post:
      summary: Create pipeline JS script
      description: Create a new pipeline JavaScript script
      tags:
      - PipelineJS
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PipelineJSInput"
      responses:
        '201':
          description: Script created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JSTargetScript"
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '409':
          description: Script already exists
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
  "/v1/pipeline-js/{name}":
    get:
      summary: Get pipeline JS script
      description: Get a specific pipeline JavaScript script by name
      tags:
      - PipelineJS
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Pipeline JS script
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JSTargetScript"
        '404':
          description: Script not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    put:
      summary: Update pipeline JS script
      description: Update an existing pipeline JavaScript script
      tags:
      - PipelineJS
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/PipelineJSInput"
      responses:
        '200':
          description: Script updated
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JSTargetScript"
        '400':
          description: Invalid input
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '404':
          description: Script not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete pipeline JS script
      description: Delete a pipeline JavaScript script
      tags:
      - PipelineJS
      parameters:
      - name: name
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Script deleted
        '404':
          description: Script not found
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
        '500':
          description: Internal server error
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorResponse"
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key for authentication. Generate via CLI with `spartan auth
        apikey generate`.
    SessionAuth:
      type: apiKey
      in: cookie
      name: spartan_session
      description: Session cookie for web UI authentication
  schemas:
    ExportSchedule:
      type: object
      required:
      - id
      - name
      - enabled
      - filters
      - export
      properties:
        id:
          type: string
          description: Unique identifier for the export schedule
        name:
          type: string
          description: Human-readable name for the schedule
        enabled:
          type: boolean
          description: Whether the schedule is active
        created_at:
          type: string
          format: date-time
          description: Creation timestamp
        updated_at:
          type: string
          format: date-time
          description: Last update timestamp
        filters:
          "$ref": "#/components/schemas/ExportFilters"
        export:
          "$ref": "#/components/schemas/ExportConfig"
        retry:
          "$ref": "#/components/schemas/ExportRetryConfig"
    ExportScheduleRequest:
      type: object
      required:
      - name
      - filters
      - export
      properties:
        name:
          type: string
          description: Human-readable name for the schedule
        enabled:
          type: boolean
          default: true
          description: Whether the schedule should be active
        filters:
          "$ref": "#/components/schemas/ExportFilters"
        export:
          "$ref": "#/components/schemas/ExportConfig"
        retry:
          "$ref": "#/components/schemas/ExportRetryConfig"
    ExportScheduleListResponse:
      type: object
      properties:
        schedules:
          type: array
          items:
            "$ref": "#/components/schemas/ExportSchedule"
    ExportFilters:
      type: object
      description: Filter criteria for matching jobs to export
      properties:
        job_kinds:
          type: array
          items:
            type: string
            enum:
            - scrape
            - crawl
            - research
          description: Job kinds to match (empty = all kinds)
        job_status:
          type: array
          items:
            type: string
            enum:
            - completed
            - failed
            - succeeded
            - canceled
          description: Job statuses to match (empty = completed only)
        tags:
          type: array
          items:
            type: string
          description: Tags that must all be present on the job
        has_results:
          type: boolean
          description: Only match jobs with non-empty results
    ExportConfig:
      type: object
      required:
      - format
      - destination_type
      properties:
        format:
          type: string
          enum:
          - json
          - jsonl
          - md
          - csv
          - xlsx
          description: Export format
        destination_type:
          type: string
          enum:
          - local
          - webhook
          description: Export destination type
        local_path:
          type: string
          description: Local file path template (for local destination)
        webhook_url:
          type: string
          format: uri
          description: Webhook URL (for webhook destination)
        path_template:
          type: string
          description: Path template with variables {job_id}, {timestamp}, {kind},
            {format}
        shape:
          "$ref": "#/components/schemas/ExportShapeConfig"
        transform:
          "$ref": "#/components/schemas/ResultTransformConfig"
    ResultExportRequest:
      type: object
      properties:
        format:
          type: string
          enum:
          - json
          - jsonl
          - md
          - csv
          - xlsx
          default: jsonl
        shape:
          "$ref": "#/components/schemas/ExportShapeConfig"
        transform:
          "$ref": "#/components/schemas/ResultTransformConfig"
    ExportShapeFormattingHints:
      type: object
      properties:
        emptyValue:
          type: string
        multiValueJoin:
          type: string
        markdownTitle:
          type: string
    ExportShapeConfig:
      type: object
      properties:
        topLevelFields:
          type: array
          items:
            type: string
        normalizedFields:
          type: array
          items:
            type: string
        evidenceFields:
          type: array
          items:
            type: string
        summaryFields:
          type: array
          items:
            type: string
        fieldLabels:
          type: object
          additionalProperties:
            type: string
        formatting:
          "$ref": "#/components/schemas/ExportShapeFormattingHints"
    ExportRetryConfig:
      type: object
      properties:
        max_retries:
          type: integer
          default: 3
          minimum: 0
          description: Maximum number of retry attempts
        base_delay_ms:
          type: integer
          default: 1000
          minimum: 100
          description: Base retry delay in milliseconds
    ExportArtifact:
      type: object
      required:
      - format
      - filename
      - contentType
      properties:
        format:
          type: string
          enum:
          - json
          - jsonl
          - md
          - csv
          - xlsx
        filename:
          type: string
        contentType:
          type: string
        recordCount:
          type: integer
        size:
          type: integer
          format: int64
        encoding:
          type: string
          enum:
          - utf8
          - base64
        content:
          type: string
          description: Inline artifact content. UTF-8 for text formats and base64 for binary formats.
    ExportFailureContext:
      type: object
      required:
      - category
      - summary
      - retryable
      - terminal
      properties:
        category:
          type: string
        summary:
          type: string
        retryable:
          type: boolean
        terminal:
          type: boolean
    ExportInspection:
      type: object
      required:
      - id
      - jobId
      - trigger
      - status
      - title
      - message
      - exportedAt
      - retryCount
      - request
      properties:
        id:
          type: string
        scheduleId:
          type: string
        jobId:
          type: string
        trigger:
          type: string
          enum:
          - api
          - cli
          - mcp
          - schedule
        status:
          type: string
          enum:
          - pending
          - succeeded
          - failed
        title:
          type: string
        message:
          type: string
        destination:
          type: string
        exportedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        retryCount:
          type: integer
        request:
          "$ref": "#/components/schemas/ResultExportRequest"
        artifact:
          "$ref": "#/components/schemas/ExportArtifact"
        failure:
          "$ref": "#/components/schemas/ExportFailureContext"
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
    ExportOutcomeResponse:
      type: object
      required:
      - export
      properties:
        export:
          "$ref": "#/components/schemas/ExportInspection"
    ExportOutcomeListResponse:
      type: object
      required:
      - exports
      - total
      - limit
      - offset
      properties:
        exports:
          type: array
          items:
            "$ref": "#/components/schemas/ExportInspection"
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    ExtractOptions:
      type: object
      properties:
        template:
          type: string
          description: Template name (e.g. "article", "product")
        validate:
          type: boolean
          default: false
          description: Validate extraction against schema if present
        ai:
          "$ref": "#/components/schemas/AIExtractOptions"
    AIExtractOptions:
      type: object
      description: AI-powered extraction options
      properties:
        enabled:
          type: boolean
          description: Enable AI-powered extraction
        mode:
          type: string
          enum:
          - natural_language
          - schema_guided
          description: AI extraction mode
        prompt:
          type: string
          description: Natural language extraction prompt (for natural_language mode)
        schema:
          type: object
          description: Schema example to guide extraction (for schema_guided mode)
        fields:
          type: array
          items:
            type: string
          description: Specific fields to extract
        use_cache:
          type: boolean
          default: true
          description: Use cached results to reduce API costs
    AIImageInput:
      type: object
      description: Ephemeral operator-supplied image context for bounded AI authoring requests. These images are used only for the current request and are not persisted as job artifacts.
      required:
      - data
      - mime_type
      properties:
        data:
          type: string
          description: Base64-encoded image payload
        mime_type:
          type: string
          enum:
          - image/png
          - image/jpeg
          - image/webp
          - image/gif
          description: Image MIME type
    AIExtractPreviewRequest:
      type: object
      description: Request for AI extraction preview. Provide either a URL to fetch or raw HTML directly.
      properties:
        url:
          type: string
          format: uri
          description: URL of the page to extract from (optional when html is provided)
        html:
          type: string
          description: HTML content to preview directly (optional when url is provided)
        mode:
          type: string
          enum:
          - natural_language
          - schema_guided
          description: AI extraction mode
        prompt:
          type: string
          description: Natural language extraction prompt
        schema:
          type: object
          description: Schema example to guide extraction
        fields:
          type: array
          items:
            type: string
          description: Specific fields to extract
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch content
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for headless fetching
        visual:
          type: boolean
          description: Capture a screenshot and include visual context when fetching the URL
    AIExtractPreviewResponse:
      type: object
      description: Response from AI extraction preview
      properties:
        fields:
          type: object
          description: Extracted fields with their values and sources
          additionalProperties:
            "$ref": "#/components/schemas/FieldValue"
        confidence:
          type: number
          minimum: 0
          maximum: 1
          description: Overall confidence score (0.0-1.0)
        explanation:
          type: string
          description: Explanation of what was extracted
        tokens_used:
          type: integer
          description: Number of tokens consumed by the LLM
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        cached:
          type: boolean
          description: Whether the result was served from cache
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
    FieldValue:
      type: object
      description: A field value with source information
      properties:
        values:
          type: array
          items:
            type: string
          description: The extracted values
        source:
          type: string
          enum:
          - selector
          - jsonld
          - regex
          - derived
          - llm
          description: Source of the extraction
        rawObject:
          type: string
          description: JSON-encoded object for complex nested values
    AIExtractTemplateGenerateRequest:
      type: object
      description: Request for AI template generation. Provide either a URL to fetch or raw HTML directly.
      required:
      - description
      properties:
        url:
          type: string
          format: uri
          description: URL to analyze for template generation (optional when html is provided)
        html:
          type: string
          description: HTML content to generate from directly (optional when url is provided)
        description:
          type: string
          description: Description of what data to extract
        sample_fields:
          type: array
          items:
            type: string
          description: Example fields that should be extracted
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch content
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for headless fetching
        visual:
          type: boolean
          description: Capture a screenshot and include visual context when fetching the URL
    AIExtractTemplateGenerateResponse:
      type: object
      description: Response from AI template generation
      properties:
        template:
          "$ref": "#/components/schemas/TemplateDetail"
        explanation:
          type: string
          description: Explanation of the generated template
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
    AIExtractTemplateDebugRequest:
      type: object
      description: Request for AI template debugging. Provide either a URL to fetch or raw HTML directly.
      required:
      - template
      properties:
        url:
          type: string
          format: uri
          description: URL to analyze for template debugging (optional when html is provided)
        html:
          type: string
          description: HTML content to debug directly (optional when url is provided)
        template:
          "$ref": "#/components/schemas/Template"
        instructions:
          type: string
          description: Optional operator guidance for the repair suggestion
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch content
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for headless fetching
        visual:
          type: boolean
          description: Capture a screenshot and include visual context when fetching the URL
    AIExtractTemplateDebugResponse:
      type: object
      description: Response from AI template debugging
      properties:
        issues:
          type: array
          items:
            type: string
          description: Deterministic local issues found while validating the template
        extracted_fields:
          type: object
          description: Current field output produced by the template against the supplied page
          additionalProperties:
            "$ref": "#/components/schemas/FieldValue"
        explanation:
          type: string
          description: Explanation of the suggested repair
        suggested_template:
          "$ref": "#/components/schemas/Template"
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
    AIRenderProfileGenerateRequest:
      type: object
      description: Request for AI render profile generation from a live URL.
      required:
      - url
      - instructions
      properties:
        url:
          type: string
          format: uri
          description: URL to inspect while generating the render profile
        name:
          type: string
          description: Optional profile name override
        host_patterns:
          type: array
          items:
            type: string
          description: Optional host patterns override for the generated profile
        instructions:
          type: string
          description: Operator guidance for the generated render profile
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch content
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for headless fetching
        visual:
          type: boolean
          description: Capture a screenshot and include visual context when fetching the URL
    AIRenderProfileGenerateResponse:
      type: object
      description: Response from AI render profile generation
      properties:
        profile:
          "$ref": "#/components/schemas/RenderProfile"
        explanation:
          type: string
          description: Explanation of the generated render profile
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
    AIRenderProfileDebugRequest:
      type: object
      description: Request for AI render profile debugging against a live URL.
      required:
      - url
      - profile
      properties:
        url:
          type: string
          format: uri
          description: URL to recheck with the current render profile
        profile:
          "$ref": "#/components/schemas/RenderProfile"
        instructions:
          type: string
          description: Optional operator guidance for tuning the profile
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch the baseline page before tuning
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for the baseline fetch
        visual:
          type: boolean
          description: Capture a screenshot and include visual context for tuning
    AIRenderProfileDebugResponse:
      type: object
      description: Response from AI render profile debugging
      properties:
        issues:
          type: array
          items:
            type: string
          description: Deterministic local issues found while rechecking the current render profile
        explanation:
          type: string
          description: Explanation of the suggested render profile changes
        suggested_profile:
          "$ref": "#/components/schemas/RenderProfile"
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
        recheck_status:
          type: integer
          description: HTTP status observed while rechecking the current render profile
        recheck_engine:
          type: string
          description: Fetch engine used while rechecking the current render profile
        recheck_error:
          type: string
          description: Error observed while rechecking the current render profile
    AIPipelineJSGenerateRequest:
      type: object
      description: Request for AI pipeline JS generation from a live URL.
      required:
      - url
      - instructions
      properties:
        url:
          type: string
          format: uri
          description: URL to inspect while generating the pipeline JS script
        name:
          type: string
          description: Optional script name override
        host_patterns:
          type: array
          items:
            type: string
          description: Optional host patterns override for the generated script
        instructions:
          type: string
          description: Operator guidance for the generated pipeline JS script
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch content
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for headless fetching
        visual:
          type: boolean
          description: Capture a screenshot and include visual context when fetching the URL
    AIPipelineJSGenerateResponse:
      type: object
      description: Response from AI pipeline JS generation
      properties:
        script:
          "$ref": "#/components/schemas/JSTargetScript"
        explanation:
          type: string
          description: Explanation of the generated pipeline JS script
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
    AIPipelineJSDebugRequest:
      type: object
      description: Request for AI pipeline JS debugging against a live URL.
      required:
      - url
      - script
      properties:
        url:
          type: string
          format: uri
          description: URL to recheck with the current pipeline JS script
        script:
          "$ref": "#/components/schemas/JSTargetScript"
        instructions:
          type: string
          description: Optional operator guidance for tuning the script
        images:
          type: array
          items:
            "$ref": "#/components/schemas/AIImageInput"
          description: Optional operator-supplied image context. These images are request-scoped and not persisted.
        headless:
          type: boolean
          description: Use headless browser to fetch the baseline page before tuning
        playwright:
          type: boolean
          description: Use Playwright instead of Chromedp for the baseline fetch
        visual:
          type: boolean
          description: Capture a screenshot and include visual context for tuning
    AIPipelineJSDebugResponse:
      type: object
      description: Response from AI pipeline JS debugging
      properties:
        issues:
          type: array
          items:
            type: string
          description: Deterministic local issues found while rechecking the current pipeline JS script
        explanation:
          type: string
          description: Explanation of the suggested pipeline JS changes
        suggested_script:
          "$ref": "#/components/schemas/JSTargetScript"
        route_id:
          type: string
          description: Exact pi route ID selected to handle the request
        provider:
          type: string
          description: Provider selected by the pi route that handled the request
        model:
          type: string
          description: Model selected by the pi route that handled the request
        visual_context_used:
          type: boolean
          description: Whether image-based visual context was included in the AI request
        recheck_status:
          type: integer
          description: HTTP status observed while rechecking the current pipeline JS script
        recheck_engine:
          type: string
          description: Fetch engine used while rechecking the current pipeline JS script
        recheck_error:
          type: string
          description: Error observed while rechecking the current pipeline JS script
    AIResearchRefineRequest:
      type: object
      description: Request for bounded research-output refinement.
      required:
      - result
      properties:
        result:
          "$ref": "#/components/schemas/ResearchResult"
        instructions:
          type: string
          description: Optional operator guidance for the refinement rewrite
    AIResearchRefineInputStats:
      type: object
      properties:
        evidenceCount:
          type: integer
        evidenceUsedCount:
          type: integer
        clusterCount:
          type: integer
        citationCount:
          type: integer
        hasAgentic:
          type: boolean
    AIResearchEvidenceHighlight:
      type: object
      required:
      - url
      - finding
      properties:
        url:
          type: string
          format: uri
        title:
          type: string
        finding:
          type: string
        relevance:
          type: string
        citationUrl:
          type: string
          format: uri
    AIResearchRefinedContent:
      type: object
      required:
      - summary
      - conciseSummary
      - keyFindings
      properties:
        summary:
          type: string
        conciseSummary:
          type: string
        keyFindings:
          type: array
          items:
            type: string
        openQuestions:
          type: array
          items:
            type: string
        recommendedNextSteps:
          type: array
          items:
            type: string
        evidenceHighlights:
          type: array
          items:
            "$ref": "#/components/schemas/AIResearchEvidenceHighlight"
        confidence:
          type: number
          minimum: 0
          maximum: 1
    AIResearchRefineResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            type: string
        inputStats:
          "$ref": "#/components/schemas/AIResearchRefineInputStats"
        refined:
          "$ref": "#/components/schemas/AIResearchRefinedContent"
        markdown:
          type: string
        explanation:
          type: string
        route_id:
          type: string
        provider:
          type: string
        model:
          type: string
    AIExportShapeRequest:
      type: object
      required:
      - job_id
      - format
      properties:
        job_id:
          type: string
          description: Representative job ID whose result file should seed the export shape.
        format:
          type: string
          enum:
          - md
          - csv
          - xlsx
          description: Target export format to shape.
        currentShape:
          "$ref": "#/components/schemas/ExportShapeConfig"
        instructions:
          type: string
          description: Optional operator guidance for the generated shape.
    AIExportShapeInputStats:
      type: object
      properties:
        fieldOptionCount:
          type: integer
        topLevelFieldCount:
          type: integer
        normalizedFieldCount:
          type: integer
        evidenceFieldCount:
          type: integer
        sampleRecordCount:
          type: integer
    AIExportShapeResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            type: string
        inputStats:
          "$ref": "#/components/schemas/AIExportShapeInputStats"
        shape:
          "$ref": "#/components/schemas/ExportShapeConfig"
        explanation:
          type: string
        route_id:
          type: string
        provider:
          type: string
        model:
          type: string
    ResultTransformConfig:
      type: object
      properties:
        expression:
          type: string
        language:
          type: string
          enum:
          - jmespath
          - jsonata
    AITransformGenerateRequest:
      type: object
      required:
      - job_id
      properties:
        job_id:
          type: string
          description: Representative job ID whose result file should seed the transform.
        currentTransform:
          "$ref": "#/components/schemas/ResultTransformConfig"
        preferredLanguage:
          type: string
          enum:
          - jmespath
          - jsonata
          description: Preferred transform language for the generated output.
        instructions:
          type: string
          description: Optional operator guidance for the generated transform.
    AITransformGenerateInputStats:
      type: object
      properties:
        sampleRecordCount:
          type: integer
        fieldPathCount:
          type: integer
        currentTransformProvided:
          type: boolean
    AITransformGenerateResponse:
      type: object
      properties:
        issues:
          type: array
          items:
            type: string
        inputStats:
          "$ref": "#/components/schemas/AITransformGenerateInputStats"
        transform:
          "$ref": "#/components/schemas/ResultTransformConfig"
        preview:
          type: array
          items: {}
        explanation:
          type: string
        route_id:
          type: string
        provider:
          type: string
        model:
          type: string
    PipelineOptions:
      type: object
      properties:
        preProcessors:
          type: array
          items:
            type: string
        postProcessors:
          type: array
          items:
            type: string
        transformers:
          type: array
          items:
            type: string
        jmesPath:
          type: string
          description: JMESPath expression for data transformation
        jsonata:
          type: string
          description: JSONata expression for data transformation
        transformVars:
          type: object
          description: Variables available to transformation expressions
          additionalProperties: true
    ResearchEvidence:
      type: object
      properties:
        url:
          type: string
        title:
          type: string
        snippet:
          type: string
        score:
          type: number
        simhash:
          type: integer
          format: uint64
        clusterId:
          type: string
        confidence:
          type: number
        citationUrl:
          type: string
        fields:
          type: object
          additionalProperties:
            "$ref": "#/components/schemas/FieldValue"
    ResearchEvidenceCluster:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        confidence:
          type: number
        evidence:
          type: array
          items:
            "$ref": "#/components/schemas/ResearchEvidence"
    ResearchCitation:
      type: object
      properties:
        url:
          type: string
        anchor:
          type: string
        canonical:
          type: string
    ResearchAgenticConfig:
      type: object
      properties:
        enabled:
          type: boolean
        instructions:
          type: string
        maxRounds:
          type: integer
          minimum: 1
          maximum: 3
        maxFollowUpUrls:
          type: integer
          minimum: 1
          maximum: 10
    ResearchAgenticRound:
      type: object
      properties:
        round:
          type: integer
        goal:
          type: string
        focusAreas:
          type: array
          items:
            type: string
        selectedUrls:
          type: array
          items:
            type: string
            format: uri
        addedEvidenceCount:
          type: integer
        reasoning:
          type: string
    ResearchAgenticResult:
      type: object
      properties:
        status:
          type: string
        instructions:
          type: string
        summary:
          type: string
        objective:
          type: string
        focusAreas:
          type: array
          items:
            type: string
        keyFindings:
          type: array
          items:
            type: string
        openQuestions:
          type: array
          items:
            type: string
        recommendedNextSteps:
          type: array
          items:
            type: string
        followUpUrls:
          type: array
          items:
            type: string
            format: uri
        rounds:
          type: array
          items:
            "$ref": "#/components/schemas/ResearchAgenticRound"
        confidence:
          type: number
        route_id:
          type: string
        provider:
          type: string
        model:
          type: string
        cached:
          type: boolean
        error:
          type: string
    ResearchResult:
      type: object
      properties:
        query:
          type: string
        summary:
          type: string
        confidence:
          type: number
        evidence:
          type: array
          items:
            "$ref": "#/components/schemas/ResearchEvidence"
        clusters:
          type: array
          items:
            "$ref": "#/components/schemas/ResearchEvidenceCluster"
        citations:
          type: array
          items:
            "$ref": "#/components/schemas/ResearchCitation"
        agentic:
          "$ref": "#/components/schemas/ResearchAgenticResult"
    HeaderKV:
      type: object
      properties:
        key:
          type: string
        value:
          type: string
    Cookie:
      type: object
      properties:
        name:
          type: string
        value:
          type: string
        domain:
          type: string
        path:
          type: string
    Token:
      type: object
      properties:
        kind:
          type: string
          enum:
          - bearer
          - basic
          - api_key
        value:
          type: string
        header:
          type: string
        query:
          type: string
        cookie:
          type: string
    LoginFlow:
      type: object
      description: Headless login flow configuration for auth profiles
      properties:
        url:
          type: string
          format: uri
          description: Login page URL
        userSelector:
          type: string
          description: CSS selector for the username or email field
        passSelector:
          type: string
          description: CSS selector for the password field
        submitSelector:
          type: string
          description: CSS selector for the submit button
        username:
          type: string
          description: Username to submit during the login flow
        password:
          type: string
          description: Password to submit during the login flow
        autoDetect:
          type: boolean
          description: Auto-detect login form fields instead of requiring selectors
        confidenceScore:
          type: number
          format: double
          description: Confidence score for auto-detected login form fields
    OAuth2Config:
      type: object
      description: OAuth 2.0 client configuration for authorization flows
      properties:
        flow_type:
          type: string
          enum:
          - authorization_code
          - client_credentials
          - device_code
          description: OAuth 2.0 flow type
        client_id:
          type: string
          description: OAuth client ID
        client_secret:
          type: string
          description: OAuth client secret (omit for public clients)
        authorize_url:
          type: string
          format: uri
          description: Authorization endpoint URL (required for authorization_code
            flow)
        token_url:
          type: string
          format: uri
          description: Token endpoint URL
        revoke_url:
          type: string
          format: uri
          description: Token revocation endpoint URL (optional)
        scopes:
          type: array
          items:
            type: string
          description: OAuth scopes to request
        use_pkce:
          type: boolean
          default: true
          description: Use PKCE for authorization code flow (required for public clients)
        redirect_uri:
          type: string
          format: uri
          description: OAuth redirect URI
        discovery_url:
          type: string
          format: uri
          description: OIDC discovery URL (.well-known/openid-configuration)
        issuer:
          type: string
          description: OIDC issuer identifier
    OAuthInitiateRequest:
      type: object
      required:
      - profile_name
      properties:
        profile_name:
          type: string
          description: Name of the profile with OAuth2 configuration
        redirect_uri:
          type: string
          format: uri
          description: Override redirect URI for this flow
    OAuthInitiateResponse:
      type: object
      properties:
        authorization_url:
          type: string
          format: uri
          description: URL to redirect user for OAuth authorization
        state:
          type: string
          description: CSRF protection state parameter
    OAuthTokenResponse:
      type: object
      properties:
        access_token:
          type: string
          description: OAuth access token
        token_type:
          type: string
          description: Token type (e.g., Bearer)
        expires_in:
          type: integer
          description: Token lifetime in seconds
        scope:
          type: string
          description: Granted scopes
    OAuthRefreshRequest:
      type: object
      required:
      - profile_name
      properties:
        profile_name:
          type: string
          description: Name of the profile to refresh token for
    OIDCDiscoverRequest:
      type: object
      description: OIDC discovery request - provide either discovery_url or issuer
      properties:
        discovery_url:
          type: string
          format: uri
          description: Full URL to .well-known/openid-configuration
        issuer:
          type: string
          description: OIDC issuer URL (will append .well-known/openid-configuration)
    OIDCProviderMetadata:
      type: object
      description: OIDC provider metadata from discovery
      properties:
        issuer:
          type: string
          description: OIDC issuer identifier
        authorization_endpoint:
          type: string
          format: uri
          description: Authorization endpoint URL
        token_endpoint:
          type: string
          format: uri
          description: Token endpoint URL
        userinfo_endpoint:
          type: string
          format: uri
          description: UserInfo endpoint URL
        revocation_endpoint:
          type: string
          format: uri
          description: Token revocation endpoint URL
        jwks_uri:
          type: string
          format: uri
          description: JSON Web Key Set URL
        scopes_supported:
          type: array
          items:
            type: string
          description: Supported OAuth scopes
        response_types_supported:
          type: array
          items:
            type: string
          description: Supported OAuth response types
        grant_types_supported:
          type: array
          items:
            type: string
          description: Supported OAuth grant types
        code_challenge_methods_supported:
          type: array
          items:
            type: string
          description: Supported PKCE code challenge methods
    TargetPreset:
      type: object
      properties:
        name:
          type: string
        hostPatterns:
          type: array
          items:
            type: string
        profile:
          type: string
        headers:
          type: array
          items:
            "$ref": "#/components/schemas/HeaderKV"
        cookies:
          type: array
          items:
            "$ref": "#/components/schemas/Cookie"
        tokens:
          type: array
          items:
            "$ref": "#/components/schemas/Token"
    AuthProfile:
      type: object
      properties:
        name:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
        parents:
          type: array
          items:
            type: string
        headers:
          type: array
          items:
            "$ref": "#/components/schemas/HeaderKV"
        cookies:
          type: array
          items:
            "$ref": "#/components/schemas/Cookie"
        tokens:
          type: array
          items:
            "$ref": "#/components/schemas/Token"
        login:
          "$ref": "#/components/schemas/LoginFlow"
        oauth2:
          "$ref": "#/components/schemas/OAuth2Config"
        presets:
          type: array
          items:
            "$ref": "#/components/schemas/TargetPreset"
    AuthVault:
      type: object
      properties:
        version:
          type: string
        profiles:
          type: array
          items:
            "$ref": "#/components/schemas/AuthProfile"
        presets:
          type: array
          items:
            "$ref": "#/components/schemas/TargetPreset"
    AuthProfilesResponse:
      type: object
      properties:
        profiles:
          type: array
          items:
            "$ref": "#/components/schemas/AuthProfile"
    AuthVaultImport:
      type: object
      properties:
        path:
          type: string
          pattern: "^[^/\\\\]+$"
          description: Simple filename only (no path, no directory separators, resolved
            relative to DATA_DIR)
      required:
      - path
    AuthVaultExport:
      type: object
      properties:
        path:
          type: string
          pattern: "^[^/\\\\]+$"
          description: Simple filename only (no path, no directory separators, resolved
            relative to DATA_DIR)
      required:
      - path
    StatusResponse:
      type: object
      properties:
        status:
          type: string
    ProxyConfig:
      type: object
      properties:
        url:
          type: string
          format: uri
          description: Proxy URL (http://, https://, socks5://)
        username:
          type: string
          description: Username for proxy authentication
        password:
          type: string
          description: Password for proxy authentication
          format: password
    DeviceCategory:
      type: string
      enum:
      - mobile
      - tablet
      - desktop
      description: Device category classification
    Orientation:
      type: string
      enum:
      - portrait
      - landscape
      description: Device screen orientation
    DeviceEmulation:
      type: object
      description: Device emulation configuration for mobile/responsive content
      properties:
        name:
          type: string
          description: Device preset name (e.g., "iPhone 14", "Pixel 7")
        viewportWidth:
          type: integer
          description: Viewport width in pixels
        viewportHeight:
          type: integer
          description: Viewport height in pixels
        deviceScaleFactor:
          type: number
          format: float
          description: Device pixel ratio (e.g., 2.0 for Retina, 3.0 for iPhone)
        userAgent:
          type: string
          description: User agent string for the device
        isMobile:
          type: boolean
          description: Whether to emulate mobile viewport
        hasTouch:
          type: boolean
          description: Whether the device has touch capability
        category:
          "$ref": "#/components/schemas/DeviceCategory"
        orientation:
          "$ref": "#/components/schemas/Orientation"
    ScreenshotConfig:
      type: object
      description: Screenshot capture configuration for headless fetchers (chromedp,
        playwright). Ignored for HTTP fetcher.
      properties:
        enabled:
          type: boolean
          default: false
          description: Whether to capture a screenshot of the page
        fullPage:
          type: boolean
          default: false
          description: Capture full page or just viewport
        format:
          type: string
          enum:
          - png
          - jpeg
          default: png
          description: Screenshot image format
        quality:
          type: integer
          minimum: 1
          maximum: 100
          default: 80
          description: JPEG quality (1-100), ignored for PNG
        width:
          type: integer
          description: Viewport width in pixels (0 = use default)
        height:
          type: integer
          description: Viewport height in pixels (0 = use default)
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
    AuthOptions:
      type: object
      properties:
        basic:
          type: string
          description: Basic auth in user:pass form
        headers:
          type: object
          additionalProperties:
            type: string
        cookies:
          type: array
          items:
            type: string
          description: Cookie strings like "name=value"
        query:
          type: object
          additionalProperties:
            type: string
          description: Query params applied to the request URL
        loginUrl:
          type: string
        loginUserSelector:
          type: string
        loginPassSelector:
          type: string
        loginSubmitSelector:
          type: string
        loginUser:
          type: string
        loginPass:
          type: string
        proxy:
          "$ref": "#/components/schemas/ProxyConfig"
        proxyHints:
          "$ref": "#/components/schemas/ProxySelectionHints"
        oauth2:
          "$ref": "#/components/schemas/OAuth2AuthConfig"
    ProxySelectionHints:
      type: object
      properties:
        preferred_region:
          type: string
          description: Prefer proxies from this region when selecting from the loaded pool
        required_tags:
          type: array
          items:
            type: string
          description: Require all of these tags on the selected proxy
        exclude_proxy_ids:
          type: array
          items:
            type: string
          description: Exclude these proxy IDs from consideration
    OAuth2AuthConfig:
      type: object
      properties:
        profileName:
          type: string
          description: Auth profile name containing OAuth2 client configuration
        accessToken:
          type: string
          description: Optional static access token override
        tokenType:
          type: string
          description: Token type to use when accessToken is provided
    WebhookConfig:
      type: object
      properties:
        url:
          type: string
          format: uri
          description: Webhook URL to receive job notifications
        events:
          type: array
          description: |
            Events to subscribe to (default: ['completed']).

            `export_completed` deliveries use the multipart export contract:
            the request body is `multipart/form-data` with a JSON `metadata`
            part and an `export` file part containing the rendered bytes.
          items:
            type: string
            enum:
            - completed
            - failed
            - canceled
            - started
            - created
            - succeeded
            - content_changed
            - page_crawled
            - retry_attempted
            - export_completed
            - all
        secret:
          type: string
          description: HMAC-SHA256 secret for signature verification
          format: password
    Watch:
      type: object
      required:
      - id
      - url
      - intervalSeconds
      - enabled
      - createdAt
      properties:
        id:
          type: string
          description: Unique watch identifier (UUID)
        url:
          type: string
          format: uri
          description: URL to monitor for changes
        selector:
          type: string
          description: CSS selector for targeted monitoring (optional)
        intervalSeconds:
          type: integer
          minimum: 60
          description: Check interval in seconds (min 60)
        enabled:
          type: boolean
          default: true
          description: Whether the watch is active
        createdAt:
          type: string
          format: date-time
          description: When the watch was created
        lastCheckedAt:
          type: string
          format: date-time
          description: When the watch was last checked
        lastChangedAt:
          type: string
          format: date-time
          description: When content last changed
        changeCount:
          type: integer
          default: 0
          description: Total number of changes detected
        diffFormat:
          type: string
          enum:
          - unified
          - html-side-by-side
          - html-inline
          default: unified
          description: Diff output format
        webhookConfig:
          "$ref": "#/components/schemas/WebhookConfig"
        notifyOnChange:
          type: boolean
          default: false
          description: Whether to send webhook notifications on change
        minChangeSize:
          type: integer
          description: Ignore changes smaller than N bytes
        ignorePatterns:
          type: array
          items:
            type: string
          description: Regex patterns to ignore in content
        headless:
          type: boolean
          default: false
          description: Use headless browser for fetching
        usePlaywright:
          type: boolean
          default: false
          description: Use Playwright instead of chromedp
        extractMode:
          type: string
          enum:
          - text
          - html
          - markdown
          description: Content extraction mode
        status:
          type: string
          enum:
          - active
          - paused
          - error
          - disabled
          description: Current watch status (derived from enabled field)
        screenshotEnabled:
          type: boolean
          default: false
          description: Enable screenshot capture and visual change detection
        screenshotConfig:
          "$ref": "#/components/schemas/ScreenshotConfig"
        visualDiffThreshold:
          type: number
          format: float
          minimum: 0
          maximum: 1
          default: 0.1
          description: Visual diff threshold (0-1) for detecting changes
        jobTrigger:
          "$ref": "#/components/schemas/JobTrigger"
    WatchInput:
      type: object
      required:
      - url
      - intervalSeconds
      properties:
        url:
          type: string
          format: uri
          description: URL to monitor for changes
        selector:
          type: string
          description: CSS selector for targeted monitoring (optional)
        intervalSeconds:
          type: integer
          minimum: 60
          default: 3600
          description: Check interval in seconds (min 60)
        enabled:
          type: boolean
          default: true
          description: Whether the watch is active
        diffFormat:
          type: string
          enum:
          - unified
          - html-side-by-side
          - html-inline
          default: unified
          description: Diff output format
        webhookConfig:
          "$ref": "#/components/schemas/WebhookConfig"
        notifyOnChange:
          type: boolean
          default: false
          description: Whether to send webhook notifications on change
        minChangeSize:
          type: integer
          description: Ignore changes smaller than N bytes
        ignorePatterns:
          type: array
          items:
            type: string
          description: Regex patterns to ignore in content
        headless:
          type: boolean
          default: false
          description: Use headless browser for fetching
        usePlaywright:
          type: boolean
          default: false
          description: Use Playwright instead of chromedp
        extractMode:
          type: string
          enum:
          - text
          - html
          - markdown
          description: Content extraction mode
        screenshotEnabled:
          type: boolean
          default: false
          description: Enable screenshot capture and visual change detection
        screenshotConfig:
          "$ref": "#/components/schemas/ScreenshotConfig"
        visualDiffThreshold:
          type: number
          format: float
          minimum: 0
          maximum: 1
          default: 0.1
          description: Visual diff threshold (0-1) for detecting changes
        jobTrigger:
          "$ref": "#/components/schemas/JobTrigger"
    JobTrigger:
      type: object
      required:
      - kind
      - request
      properties:
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
          description: Job kind to submit when the watch detects a change
        request:
          type: object
          description: Operator-facing request payload matching the selected job kind
    WatchList:
      type: object
      required:
      - watches
      - total
      - limit
      - offset
      properties:
        watches:
          type: array
          items:
            "$ref": "#/components/schemas/Watch"
          description: Page of configured watches.
        total:
          type: integer
          description: Total number of configured watches before pagination.
        limit:
          type: integer
          description: Requested page size.
        offset:
          type: integer
          description: Requested page offset.
    WatchArtifact:
      type: object
      required:
      - kind
      - filename
      - contentType
      - downloadUrl
      properties:
        kind:
          type: string
          enum:
          - current-screenshot
          - previous-screenshot
          - visual-diff
          description: Persisted artifact kind from the completed watch check
        filename:
          type: string
          description: Suggested download filename for the artifact
        contentType:
          type: string
          description: MIME type served by the artifact download endpoint
        byteSize:
          type: integer
          format: int64
          description: Persisted artifact size in bytes
        downloadUrl:
          type: string
          description: Relative API URL for downloading the artifact bytes
    WatchCheckInspection:
      type: object
      required:
      - id
      - watchId
      - url
      - checkedAt
      - status
      - changed
      - title
      - message
      - visualChanged
      properties:
        id:
          type: string
          description: Persisted watch check id
        watchId:
          type: string
          description: ID of the watch that was checked
        url:
          type: string
          format: uri
          description: URL that was checked
        checkedAt:
          type: string
          format: date-time
          description: When the check completed
        status:
          type: string
          enum:
          - baseline
          - changed
          - unchanged
          - failed
          description: High-level watch check outcome
        changed:
          type: boolean
          description: Whether this check detected a content or visual change
        baseline:
          type: boolean
          description: Whether this check established the first persisted comparison baseline
        title:
          type: string
          description: Short operator-facing inspection title
        message:
          type: string
          description: Guided operator-facing summary for the check
        previousHash:
          type: string
          description: Previous content hash when available
        currentHash:
          type: string
          description: Current content hash when available
        diffText:
          type: string
          description: Unified diff text when textual content changed
        diffHtml:
          type: string
          description: HTML diff when textual content changed
        error:
          type: string
          description: Error summary when the check failed or follow-on automation had issues
        selector:
          type: string
          description: CSS selector used for extraction
        artifacts:
          type: array
          items:
            "$ref": "#/components/schemas/WatchArtifact"
          description: Check-scoped artifact snapshots exposed through explicit download URLs
        visualHash:
          type: string
          description: Perceptual hash of current screenshot
        previousVisualHash:
          type: string
          description: Perceptual hash of previous screenshot
        visualChanged:
          type: boolean
          description: Whether visual changes were detected
        visualSimilarity:
          type: number
          format: float
          description: Similarity score between screenshots (0-1)
        triggeredJobs:
          type: array
          items:
            type: string
          description: Job IDs created from the optional watch trigger during this check
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
          description: Guided next steps for follow-up inspection or retry
    WatchCheckInspectionResponse:
      type: object
      required:
      - check
      properties:
        check:
          "$ref": "#/components/schemas/WatchCheckInspection"
    WatchCheckHistoryResponse:
      type: object
      required:
      - checks
      - total
      - limit
      - offset
      properties:
        checks:
          type: array
          items:
            "$ref": "#/components/schemas/WatchCheckInspection"
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    WebhookPayload:
      type: object
      description: |
        Webhook event metadata.

        Non-export events are delivered as a JSON request body matching this schema.
        `export.completed` deliveries use `multipart/form-data` with a `metadata`
        part containing this schema and an `export` file part containing the
        rendered export bytes.
      properties:
        eventId:
          type: string
          description: Unique identifier for this event
        eventType:
          type: string
          enum:
          - job.created
          - job.started
          - job.completed
          - content.changed
          - page.crawled
          - retry.attempted
          - export.completed
          - visual.changed
          description: Type of event that triggered this webhook
        timestamp:
          type: string
          format: date-time
          description: ISO 8601 timestamp of the event
        jobId:
          type: string
          description: ID of the job this event relates to
        jobKind:
          type: string
          enum:
          - scrape
          - crawl
          - research
          description: Type of job
        status:
          type: string
          enum:
          - queued
          - running
          - succeeded
          - failed
          - canceled
          description: Current job status
        prevStatus:
          type: string
          description: Previous job status (if applicable)
        error:
          type: string
          description: Error message if job failed
        resultUrl:
          type: string
          description: Relative API URL for retrieving persisted job results
        completedAt:
          type: string
          format: date-time
          description: Timestamp when job completed (for completed events)
        url:
          type: string
          description: URL where content changed
        previousHash:
          type: string
          description: Previous content hash
        currentHash:
          type: string
          description: Current content hash
        diffText:
          type: string
          description: Text diff of changes
        diffHtml:
          type: string
          description: HTML diff of changes
        selector:
          type: string
          description: CSS selector for tracked element
        visualHash:
          type: string
          description: Perceptual hash of current screenshot
        previousVisualHash:
          type: string
          description: Perceptual hash of previous screenshot
        visualSimilarity:
          type: number
          format: float
          description: Visual similarity score (0-1)
        pageUrl:
          type: string
          description: URL of crawled page
        pageStatus:
          type: integer
          description: HTTP status code of crawled page
        pageTitle:
          type: string
          description: Title of crawled page
        pageDepth:
          type: integer
          description: Crawl depth of the page
        isDuplicate:
          type: boolean
          description: Whether this page is a duplicate
        duplicateOf:
          type: string
          description: URL of original page if this is a duplicate
        crawlSeqNum:
          type: integer
          description: Sequence number in crawl order
        attemptNumber:
          type: integer
          description: Current retry attempt number
        maxAttempts:
          type: integer
          description: Maximum number of retry attempts
        retryError:
          type: string
          description: Error that triggered the retry
        fetcherType:
          type: string
          description: Type of fetcher being used
        exportFormat:
          type: string
          description: Export format (json, csv, etc.)
        filename:
          type: string
          description: Delivered export filename (for export.completed)
        contentType:
          type: string
          description: Delivered export content type (for export.completed)
        recordCount:
          type: integer
          description: Number of records exported
        exportSize:
          type: integer
          format: int64
          description: Size of exported file in bytes
    WebhookExportDelivery:
      type: object
      description: |
        Multipart webhook delivery used when `eventType` is `export.completed`.
        The `metadata` part is JSON matching `WebhookPayload`; the `export` part
        contains the rendered export bytes.
      properties:
        metadata:
          "$ref": "#/components/schemas/WebhookPayload"
        export:
          type: string
          format: binary
          description: Rendered export bytes
    ScrapeRequest:
      type: object
      required:
      - url
      properties:
        url:
          type: string
          format: uri
        method:
          type: string
          enum:
          - GET
          - POST
          - PUT
          - DELETE
          - PATCH
          - HEAD
          - OPTIONS
          default: GET
          description: HTTP method to use for the request
        body:
          type: string
          description: Request body for POST/PUT/PATCH requests (use base64 for binary
            data)
        contentType:
          type: string
          description: Content-Type header for the request body (auto-detected if
            not provided)
        headless:
          type: boolean
          default: false
        playwright:
          type: boolean
          default: false
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
        incremental:
          type: boolean
          default: false
        proxy:
          "$ref": "#/components/schemas/ProxyConfig"
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
    CrawlRequest:
      type: object
      required:
      - url
      properties:
        url:
          type: string
          format: uri
        maxDepth:
          type: integer
          default: 2
          minimum: 1
          maximum: 10
        maxPages:
          type: integer
          default: 200
          minimum: 1
          maximum: 10000
        headless:
          type: boolean
          default: false
        playwright:
          type: boolean
          default: false
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
        incremental:
          type: boolean
          default: false
        proxy:
          "$ref": "#/components/schemas/ProxyConfig"
        sitemapURL:
          type: string
          format: uri
          description: Optional URL to a sitemap.xml file. If provided, URLs from
            the sitemap will be used as crawl seeds.
        sitemapOnly:
          type: boolean
          default: false
          description: If true, only crawl URLs from the sitemap, not the root URL.
            Requires sitemapURL to be set.
        includePatterns:
          type: array
          items:
            type: string
          description: URL path patterns to include (glob syntax, e.g., /blog/**).
            If specified, only matching URLs are crawled.
        excludePatterns:
          type: array
          items:
            type: string
          description: URL path patterns to exclude (glob syntax, e.g., /admin/*).
            Excluded URLs take precedence over included ones.
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
        skipDuplicates:
          type: boolean
          default: false
          description: Enable near-duplicate content detection during crawling
        simHashThreshold:
          type: integer
          default: 3
          minimum: 0
          maximum: 64
          description: Maximum Hamming distance for content to be considered a duplicate
            (within same crawl)
        crossJobDedup:
          type: boolean
          default: false
          description: Enable cross-job duplicate detection using content index
        crossJobDedupThreshold:
          type: integer
          default: 3
          minimum: 0
          maximum: 64
          description: Hamming distance threshold for cross-job duplicate detection
    ResearchRequest:
      type: object
      required:
      - query
      - urls
      properties:
        query:
          type: string
        urls:
          type: array
          items:
            type: string
            format: uri
        maxDepth:
          type: integer
          default: 2
          minimum: 1
          maximum: 10
        maxPages:
          type: integer
          default: 200
          minimum: 1
          maximum: 10000
        headless:
          type: boolean
          default: false
        playwright:
          type: boolean
          default: false
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
        proxy:
          "$ref": "#/components/schemas/ProxyConfig"
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
        agentic:
          "$ref": "#/components/schemas/ResearchAgenticConfig"
    Job:
      type: object
      required:
      - id
      - kind
      - status
      - createdAt
      - updatedAt
      - specVersion
      - spec
      - run
      properties:
        id:
          type: string
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
        status:
          type: string
          enum:
          - queued
          - running
          - succeeded
          - failed
          - canceled
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        startedAt:
          type: string
          format: date-time
          description: When the job first started running.
        finishedAt:
          type: string
          format: date-time
          description: When the job reached a terminal state.
        specVersion:
          type: integer
          description: Persisted typed job spec version.
        spec:
          type: object
          description: Persisted typed job spec. Sensitive fields are redacted in untrusted responses.
        error:
          type: string
          description: Error message if job failed. Secrets and filesystem paths are
            redacted.
        interceptedData:
          type: array
          items:
            "$ref": "#/components/schemas/InterceptedEntry"
          description: Captured network activity if network interception was enabled
        dependsOn:
          type: array
          items:
            type: string
          description: List of job IDs this job depends on
        dependencyStatus:
          type: string
          enum:
          - pending
          - ready
          - failed
          description: Status of dependency resolution
        chainId:
          type: string
          description: ID of the job chain this job belongs to, if any
        selectedEngine:
          type: string
          description: Execution engine chosen for the job.
        run:
          "$ref": "#/components/schemas/JobRunSummary"
    JobRunSummary:
      type: object
      required:
      - waitMs
      - runMs
      - totalMs
      properties:
        waitMs:
          type: integer
          format: int64
          description: Time spent waiting in the queue before the run started or ended.
        runMs:
          type: integer
          format: int64
          description: Time spent actively running.
        totalMs:
          type: integer
          format: int64
          description: Total elapsed time from creation to the current or finished state.
        queue:
          "$ref": "#/components/schemas/JobQueueProgress"
        failure:
          "$ref": "#/components/schemas/JobFailureContext"
    JobQueueProgress:
      type: object
      properties:
        batchId:
          type: string
        index:
          type: integer
          description: One-based position of the job inside the persisted batch.
        total:
          type: integer
          description: Total number of jobs in the batch.
        completed:
          type: integer
          description: Number of terminal jobs in the batch.
        remaining:
          type: integer
          description: Number of jobs not yet terminal in the batch.
        queued:
          type: integer
          description: Number of queued jobs in the batch.
        running:
          type: integer
          description: Number of running jobs in the batch.
        percent:
          type: integer
          description: Batch completion percent based on terminal jobs.
    JobFailureContext:
      type: object
      required:
      - category
      - summary
      - retryable
      - terminal
      properties:
        category:
          type: string
          description: Derived failure classification for operator triage.
        summary:
          type: string
          description: Sanitized terminal failure summary.
        retryable:
          type: boolean
          description: Whether the failure looks transient enough to retry.
        terminal:
          type: boolean
          description: Whether the reported failure is terminal for the recorded run.
    JobResponse:
      type: object
      required:
      - job
      properties:
        job:
          "$ref": "#/components/schemas/Job"
    JobList:
      type: object
      required:
      - jobs
      - total
      - limit
      - offset
      properties:
        jobs:
          type: array
          items:
            "$ref": "#/components/schemas/Job"
        total:
          type: integer
          description: Total number of jobs matching the filter.
        limit:
          type: integer
          description: Page size used for this response.
        offset:
          type: integer
          description: Pagination offset used for this response.
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
          - ok
          - degraded
          - setup_required
          - error
        version:
          type: string
        components:
          type: object
          additionalProperties:
            "$ref": "#/components/schemas/ComponentStatus"
        notices:
          type: array
          items:
            "$ref": "#/components/schemas/RuntimeNotice"
        setup:
          "$ref": "#/components/schemas/SetupStatus"
      required:
      - status
      - version
      - components
    RecommendedAction:
      type: object
      properties:
        label:
          type: string
        kind:
          type: string
          enum:
          - route
          - command
          - env
          - copy
          - doc
          - external-link
          - one-click
        value:
          type: string
      required:
      - label
      - kind
    RuntimeNotice:
      type: object
      properties:
        id:
          type: string
        scope:
          type: string
        severity:
          type: string
          enum:
          - info
          - warning
          - error
        title:
          type: string
        message:
          type: string
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
      required:
      - id
      - scope
      - severity
      - title
      - message
    SetupStatus:
      type: object
      properties:
        required:
          type: boolean
        code:
          type: string
        title:
          type: string
        message:
          type: string
        dataDir:
          type: string
        schemaVersion:
          type: string
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
      required:
      - required
    ComponentStatus:
      type: object
      properties:
        status:
          type: string
          enum:
          - ok
          - degraded
          - disabled
          - setup_required
          - error
        message:
          type: string
        details:
          type: object
          additionalProperties: {}
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
      required:
      - status
    DiagnosticActionResponse:
      type: object
      properties:
        status:
          type: string
          enum:
          - ok
          - degraded
          - disabled
          - error
        title:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: {}
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
      required:
      - status
      - message
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
          description: Human-readable error message
      required:
      - error
    CapabilityGuidance:
      type: object
      properties:
        status:
          type: string
          enum:
          - disabled
          - warning
          - danger
          - ok
        title:
          type: string
        message:
          type: string
        actions:
          type: array
          items:
            "$ref": "#/components/schemas/RecommendedAction"
      required:
      - status
    RetentionStatusResponse:
      type: object
      properties:
        enabled:
          type: boolean
          description: Whether retention is enabled
        jobRetentionDays:
          type: integer
          description: Max age for jobs in days (0 = unlimited)
        crawlStateDays:
          type: integer
          description: Max age for crawl states in days (0 = unlimited)
        maxJobs:
          type: integer
          description: Max total jobs to keep (0 = unlimited)
        maxStorageGB:
          type: integer
          description: Max storage in GB (0 = unlimited)
        totalJobs:
          type: integer
          format: int64
          description: Current total job count
        jobsEligible:
          type: integer
          format: int64
          description: Jobs eligible for cleanup
        storageUsedMB:
          type: integer
          format: int64
          description: Current storage usage in MB
        guidance:
          "$ref": "#/components/schemas/CapabilityGuidance"
      required:
      - enabled
      - jobRetentionDays
      - crawlStateDays
      - maxJobs
      - maxStorageGB
      - totalJobs
      - jobsEligible
      - storageUsedMB
    RetentionCleanupRequest:
      type: object
      properties:
        dryRun:
          type: boolean
          description: Preview only, don't delete
          default: true
        force:
          type: boolean
          description: Run cleanup even if retention is disabled
        olderThan:
          type: integer
          description: Override age threshold (days)
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
          description: Only cleanup specific job kind
      required:
      - dryRun
    RetentionCleanupResponse:
      type: object
      properties:
        jobsDeleted:
          type: integer
          description: Number of jobs deleted
        jobsAttempted:
          type: integer
          description: Total jobs attempted to delete
        crawlStatesDeleted:
          type: integer
          format: int64
          description: Number of crawl states deleted
        spaceReclaimedMB:
          type: integer
          format: int64
          description: Space reclaimed in MB
        durationMs:
          type: integer
          format: int64
          description: Operation duration in milliseconds
        failedJobIDs:
          type: array
          items:
            type: string
          description: Jobs whose artifacts failed to delete
        errors:
          type: array
          items:
            type: string
          description: Error messages encountered
        dryRun:
          type: boolean
          description: Whether this was a dry-run
      required:
      - jobsDeleted
      - jobsAttempted
      - crawlStatesDeleted
      - spaceReclaimedMB
      - durationMs
      - dryRun
    ProxyStatus:
      type: object
      properties:
        id:
          type: string
        region:
          type: string
        tags:
          type: array
          items:
            type: string
        is_healthy:
          type: boolean
        request_count:
          type: integer
          format: int64
        success_count:
          type: integer
          format: int64
        failure_count:
          type: integer
          format: int64
        success_rate:
          type: number
          format: double
        avg_latency_ms:
          type: integer
          format: int64
        consecutive_fails:
          type: integer
      required:
      - id
      - is_healthy
      - request_count
      - success_count
      - failure_count
      - success_rate
      - avg_latency_ms
      - consecutive_fails
    ProxyPoolStatusResponse:
      type: object
      properties:
        strategy:
          type: string
        total_proxies:
          type: integer
          format: int64
        healthy_proxies:
          type: integer
          format: int64
        regions:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string
        proxies:
          type: array
          items:
            "$ref": "#/components/schemas/ProxyStatus"
      required:
      - strategy
      - total_proxies
      - healthy_proxies
      - regions
      - tags
      - proxies
    Schedule:
      type: object
      properties:
        id:
          type: string
          description: Unique schedule identifier
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
        intervalSeconds:
          type: integer
          minimum: 1
          description: Interval between runs in seconds
        nextRun:
          type: string
          format: date-time
          description: Next scheduled run time (RFC3339)
        request:
          description: Operator-facing job request matching the selected kind.
          oneOf:
          - "$ref": "#/components/schemas/ScrapeRequest"
          - "$ref": "#/components/schemas/CrawlRequest"
          - "$ref": "#/components/schemas/ResearchRequest"
      required:
      - id
      - kind
      - intervalSeconds
      - nextRun
      - request
    ScheduleRequest:
      type: object
      required:
      - kind
      - intervalSeconds
      - request
      properties:
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
        intervalSeconds:
          type: integer
          minimum: 1
          description: Interval between runs in seconds
        request:
          description: Job request matching the selected kind.
          oneOf:
          - "$ref": "#/components/schemas/ScrapeRequest"
          - "$ref": "#/components/schemas/CrawlRequest"
          - "$ref": "#/components/schemas/ResearchRequest"
    ScheduleListResponse:
      type: object
      properties:
        schedules:
          type: array
          items:
            "$ref": "#/components/schemas/Schedule"
      required:
      - schedules
    CrawlState:
      type: object
      required:
      - url
      - etag
      - lastModified
      - contentHash
      - lastScraped
      properties:
        url:
          type: string
          description: URL that was crawled
        etag:
          type: string
          description: ETag header value for incremental detection
        lastModified:
          type: string
          description: Last-Modified header value
        contentHash:
          type: string
          description: Hash of content for incremental detection
        lastScraped:
          type: string
          format: date-time
          description: Timestamp of last scrape
        depth:
          type: integer
          description: Depth at which this URL was found during crawl
        jobId:
          type: string
          description: ID of the job that last scraped this URL
    DuplicateMatch:
      type: object
      description: A potential duplicate content match from another job
      required:
      - jobId
      - url
      - simhash
      - distance
      - indexedAt
      properties:
        jobId:
          type: string
          description: ID of the job that indexed this content
        url:
          type: string
          description: URL of the duplicate content
        simhash:
          type: integer
          format: uint64
          description: Simhash fingerprint of the content
        distance:
          type: integer
          description: Hamming distance from the query simhash (lower = more similar)
        indexedAt:
          type: string
          format: date-time
          description: When this content was indexed
    ContentEntry:
      type: object
      description: A single indexed content fingerprint
      required:
      - jobId
      - simhash
      - indexedAt
      properties:
        jobId:
          type: string
          description: ID of the job that indexed this content
        simhash:
          type: integer
          format: uint64
          description: Simhash fingerprint of the content
        indexedAt:
          type: string
          format: date-time
          description: When this content was indexed
    DedupStats:
      type: object
      description: Deduplication statistics
      required:
      - totalIndexed
      - uniqueUrls
      - uniqueJobs
      - duplicatePairs
      properties:
        totalIndexed:
          type: integer
          description: Total number of content fingerprints indexed
        uniqueUrls:
          type: integer
          description: Number of unique URLs indexed
        uniqueJobs:
          type: integer
          description: Number of unique jobs that have indexed content
        duplicatePairs:
          type: integer
          description: Number of URLs that appear in multiple jobs
    WSMessage:
      type: object
      description: WebSocket message envelope
      required:
      - type
      - timestamp
      - payload
      properties:
        type:
          type: string
          description: Message type
          enum:
          - job_created
          - job_started
          - job_status_changed
          - job_completed
          - manager_status
          - metrics
          - ping
          - pong
          - subscribe_jobs
          - unsubscribe_jobs
        timestamp:
          type: integer
          format: int64
          description: Unix timestamp in milliseconds
        payload:
          description: Message payload (varies by type)
    WSJobEventPayload:
      type: object
      description: Payload for job-related WebSocket events
      required:
      - jobId
      - kind
      - status
      - updatedAt
      properties:
        jobId:
          type: string
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
        status:
          type: string
          enum:
          - queued
          - running
          - succeeded
          - failed
          - canceled
        error:
          type: string
          description: Error message if job failed
        progress:
          "$ref": "#/components/schemas/WSJobProgress"
        updatedAt:
          type: integer
          format: int64
          description: Unix timestamp in milliseconds
    WSJobProgress:
      type: object
      description: Progress information for crawl jobs
      properties:
        pagesCrawled:
          type: integer
        pagesTotal:
          type: integer
        depthCurrent:
          type: integer
        depthMax:
          type: integer
    WSManagerStatusPayload:
      type: object
      description: Payload for manager status updates
      required:
      - queuedJobs
      - activeJobs
      properties:
        queuedJobs:
          type: integer
          description: Number of jobs in queue
        activeJobs:
          type: integer
          description: Number of currently running jobs
    MetricsResponse:
      type: object
      description: Performance metrics and rate limiting status
      properties:
        requestsPerSec:
          type: number
          format: float
          description: Requests per second over the retention window
        successRate:
          type: number
          format: float
          description: Success rate as percentage (0-100)
        avgResponseTimeMs:
          type: number
          format: float
          description: Average response time in milliseconds
        activeRequests:
          type: integer
          description: Number of currently active requests
        totalRequests:
          type: integer
          format: int64
          description: Total number of requests since startup
        fetcherUsage:
          "$ref": "#/components/schemas/FetcherUsageBreakdown"
        rateLimitStatus:
          type: array
          items:
            "$ref": "#/components/schemas/RateLimitStatus"
          description: Rate limit status for all known hosts
        jobThroughputPerMin:
          type: number
          format: float
          description: Job throughput in jobs per minute
        avgJobDurationMs:
          type: number
          format: float
          description: Average job duration in milliseconds
        timestamp:
          type: integer
          format: int64
          description: Unix timestamp when metrics were captured
    FetcherUsageBreakdown:
      type: object
      description: Usage counts per fetcher type
      properties:
        http:
          type: integer
          format: int64
          description: Number of HTTP requests
        chromedp:
          type: integer
          format: int64
          description: Number of Chromedp requests
        playwright:
          type: integer
          format: int64
          description: Number of Playwright requests
    RateLimitStatus:
      type: object
      description: Rate limit status for a single host
      properties:
        host:
          type: string
          description: Hostname (sanitized, no paths or query params)
        qps:
          type: number
          format: float
          description: Queries per second limit
        burst:
          type: integer
          description: Burst capacity
        tokens:
          type: number
          format: float
          description: Estimated current tokens available
        lastRequest:
          type: integer
          format: int64
          description: Unix timestamp of last request to this host
    RenderProfile:
      type: object
      description: Render profile configuration for matching hosts
      required:
      - name
      - hostPatterns
      properties:
        name:
          type: string
          description: Unique profile name
        hostPatterns:
          type: array
          items:
            type: string
          description: Host patterns to match (e.g., "example.com", "*.example.com")
        forceEngine:
          type: string
          enum:
          - http
          - chromedp
          - playwright
          description: Force specific rendering engine
        preferHeadless:
          type: boolean
          description: Skip HTTP probe and use headless engine
        assumeJsHeavy:
          type: boolean
          description: Treat all pages as JS-heavy
        neverHeadless:
          type: boolean
          description: Never use headless rendering
        jsHeavyThreshold:
          type: number
          format: float
          description: JS heaviness threshold (0-1)
        rateLimitQPS:
          type: integer
          description: Rate limit QPS for matching hosts (0 = use global default)
        rateLimitBurst:
          type: integer
          description: Rate limit burst for matching hosts (0 = use global default)
        block:
          type: object
          properties:
            resourceTypes:
              type: array
              items:
                type: string
                enum:
                - image
                - media
                - font
                - stylesheet
                - other
            urlPatterns:
              type: array
              items:
                type: string
        wait:
          type: object
          properties:
            mode:
              type: string
              enum:
              - dom_ready
              - network_idle
              - stability
              - selector
            selector:
              type: string
            networkIdleQuietMs:
              type: integer
            minTextLength:
              type: integer
            stabilityPollMs:
              type: integer
            stabilityIterations:
              type: integer
            extraSleepMs:
              type: integer
        timeouts:
          type: object
          properties:
            maxRenderMs:
              type: integer
            scriptEvalMs:
              type: integer
            navigationMs:
              type: integer
        screenshot:
          type: object
          properties:
            enabled:
              type: boolean
            fullPage:
              type: boolean
            format:
              type: string
              enum:
              - png
              - jpeg
            quality:
              type: integer
            width:
              type: integer
            height:
              type: integer
        device:
          type: object
          properties:
            name:
              type: string
            viewportWidth:
              type: integer
            viewportHeight:
              type: integer
            deviceScaleFactor:
              type: number
              format: float
            userAgent:
              type: string
            isMobile:
              type: boolean
            hasTouch:
              type: boolean
        captchaConfig:
          "$ref": "#/components/schemas/CaptchaConfig"
    RenderProfilesResponse:
      type: object
      properties:
        profiles:
          type: array
          items:
            "$ref": "#/components/schemas/RenderProfile"
    RenderProfileInput:
      type: object
      required:
      - name
      - hostPatterns
      properties:
        name:
          type: string
          description: Unique profile name
        hostPatterns:
          type: array
          items:
            type: string
          description: Host patterns to match
        forceEngine:
          type: string
          enum:
          - http
          - chromedp
          - playwright
          description: Force a specific rendering engine
        preferHeadless:
          type: boolean
          description: Skip HTTP probe and use headless
        neverHeadless:
          type: boolean
          description: Force HTTP, never use headless
        assumeJsHeavy:
          type: boolean
          description: Treat all pages as JS-heavy
        jsHeavyThreshold:
          type: number
          minimum: 0
          maximum: 1
          description: JS-heavy threshold (0-1, 0=use default)
        rateLimitQPS:
          type: integer
          minimum: 0
          description: Rate limit QPS (0=use default)
        rateLimitBurst:
          type: integer
          minimum: 0
          description: Rate limit burst (0=use default)
        block:
          type: object
          properties:
            resourceTypes:
              type: array
              items:
                type: string
                enum:
                - image
                - media
                - font
                - stylesheet
                - other
            urlPatterns:
              type: array
              items:
                type: string
        wait:
          type: object
          properties:
            mode:
              type: string
              enum:
              - dom_ready
              - network_idle
              - stability
              - selector
            selector:
              type: string
            networkIdleQuietMs:
              type: integer
            minTextLength:
              type: integer
            stabilityPollMs:
              type: integer
            stabilityIterations:
              type: integer
            extraSleepMs:
              type: integer
        timeouts:
          type: object
          properties:
            maxRenderMs:
              type: integer
            scriptEvalMs:
              type: integer
            navigationMs:
              type: integer
        screenshot:
          type: object
          properties:
            enabled:
              type: boolean
            fullPage:
              type: boolean
            format:
              type: string
              enum:
              - png
              - jpeg
            quality:
              type: integer
            width:
              type: integer
            height:
              type: integer
        device:
          type: object
          properties:
            name:
              type: string
            viewportWidth:
              type: integer
            viewportHeight:
              type: integer
            deviceScaleFactor:
              type: number
            userAgent:
              type: string
            isMobile:
              type: boolean
            hasTouch:
              type: boolean
    PipelineJSResponse:
      type: object
      properties:
        scripts:
          type: array
          items:
            "$ref": "#/components/schemas/JSTargetScript"
    JSTargetScript:
      type: object
      required:
      - name
      - hostPatterns
      properties:
        name:
          type: string
          description: Unique script name
        hostPatterns:
          type: array
          items:
            type: string
          description: Host patterns to match
        engine:
          type: string
          enum:
          - chromedp
          - playwright
          description: Target engine for this script
        preNav:
          type: string
          description: JavaScript to execute before navigation
        postNav:
          type: string
          description: JavaScript to execute after navigation
        selectors:
          type: array
          items:
            type: string
          description: CSS selectors to wait for
    PipelineJSInput:
      type: object
      required:
      - name
      - hostPatterns
      properties:
        name:
          type: string
          description: Unique script name
        hostPatterns:
          type: array
          items:
            type: string
          description: Host patterns to match
        engine:
          type: string
          enum:
          - chromedp
          - playwright
          description: Target engine for this script
        preNav:
          type: string
          description: JavaScript to execute before navigation
        postNav:
          type: string
          description: JavaScript to execute after navigation
        selectors:
          type: array
          items:
            type: string
          description: CSS selectors to wait for
    CaptchaConfig:
      type: object
      description: Configuration for CAPTCHA detection and solving
      properties:
        enabled:
          type: boolean
          description: Enable CAPTCHA detection
          default: false
        autoSolve:
          type: boolean
          description: Automatically solve detected CAPTCHAs
          default: false
        service:
          type: string
          description: Solving service to use
          enum:
          - 2captcha
          - anticaptcha
        apiKey:
          type: string
          description: API key for the solving service (sensitive)
        customEndpoint:
          type: string
          description: Optional custom service endpoint
        maxRetries:
          type: integer
          description: Maximum solving attempts
          default: 3
        retryDelay:
          type: integer
          description: Delay between retries in seconds
          default: 5
        timeout:
          type: integer
          description: Maximum time to wait for solution in seconds
          default: 120
        pollingInterval:
          type: integer
          description: Poll interval for solution in seconds
          default: 5
        minConfidence:
          type: number
          format: float
          description: Minimum detection confidence (0.0-1.0)
          default: 0.7
    NetworkInterceptConfig:
      type: object
      description: Configuration for network request/response interception for API
        scraping
      properties:
        enabled:
          type: boolean
          default: false
          description: Enable network interception
        urlPatterns:
          type: array
          items:
            type: string
          description: Glob patterns for URLs to intercept (e.g., "**/api/**", "*.json")
        resourceTypes:
          type: array
          items:
            type: string
            enum:
            - xhr
            - fetch
            - document
            - script
            - stylesheet
            - image
            - media
            - font
            - websocket
            - other
          description: Resource types to capture
        captureRequestBody:
          type: boolean
          default: true
          description: Whether to capture request bodies
        captureResponseBody:
          type: boolean
          default: true
          description: Whether to capture response bodies
        maxBodySize:
          type: integer
          default: 1048576
          description: Maximum bytes to capture per body (default 1MB)
        maxEntries:
          type: integer
          default: 1000
          description: Maximum number of entries to capture
    InterceptedEntry:
      type: object
      description: Captured network request/response pair
      properties:
        request:
          "$ref": "#/components/schemas/InterceptedRequest"
        response:
          "$ref": "#/components/schemas/InterceptedResponse"
        duration:
          type: integer
          description: Time between request and response in milliseconds
    InterceptedRequest:
      type: object
      description: Captured network request
      properties:
        requestId:
          type: string
          description: Unique identifier
        url:
          type: string
          description: Request URL
        method:
          type: string
          description: HTTP method
        headers:
          type: object
          additionalProperties:
            type: string
          description: Request headers
        body:
          type: string
          description: Request body (base64 if binary)
        bodySize:
          type: integer
          description: Original body size in bytes
        timestamp:
          type: string
          format: date-time
          description: When request was sent
        resourceType:
          type: string
          enum:
          - xhr
          - fetch
          - document
          - script
          - stylesheet
          - image
          - media
          - font
          - websocket
          - other
          description: Type of resource
    InterceptedResponse:
      type: object
      description: Captured network response
      properties:
        requestId:
          type: string
          description: Matches request
        status:
          type: integer
          description: HTTP status code
        statusText:
          type: string
          description: HTTP status text
        headers:
          type: object
          additionalProperties:
            type: string
          description: Response headers
        body:
          type: string
          description: Response body (base64 if binary)
        bodySize:
          type: integer
          description: Size of response body in bytes
        timestamp:
          type: string
          format: date-time
          description: When response was received
    BatchJobRequest:
      type: object
      description: A single job within a batch request
      required:
      - url
      properties:
        url:
          type: string
          format: uri
          description: URL to scrape/crawl/research
        method:
          type: string
          enum:
          - GET
          - POST
          - PUT
          - DELETE
          - PATCH
          - HEAD
          - OPTIONS
          default: GET
          description: HTTP method for the request
        body:
          type: string
          description: Request body for POST/PUT/PATCH requests
        contentType:
          type: string
          description: Content-Type header for the request body
        headers:
          type: object
          additionalProperties:
            type: string
          description: Additional headers to include in the request
    BatchScrapeRequest:
      type: object
      description: Request to create multiple scrape jobs
      required:
      - jobs
      properties:
        jobs:
          type: array
          minItems: 1
          maxItems: 100
          items:
            "$ref": "#/components/schemas/BatchJobRequest"
          description: List of jobs to create (1-100)
        outputFormat:
          type: string
          enum:
          - jsonl
          - json
          - md
          - csv
          - xlsx
          default: jsonl
          description: Output format for results
        extractionName:
          type: string
          description: Template name for extraction (e.g., "article", "product")
        extractionMode:
          type: string
          description: Extraction mode
        headless:
          type: boolean
          default: false
          description: Use headless browser
        playwright:
          type: boolean
          default: false
          description: Use Playwright instead of Chromedp
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
          description: Request timeout in seconds
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
          description: Named auth profile to use
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        incremental:
          type: boolean
          default: false
          description: Enable incremental scraping
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
    BatchCrawlRequest:
      type: object
      description: Request to create multiple crawl jobs
      required:
      - jobs
      properties:
        jobs:
          type: array
          minItems: 1
          maxItems: 100
          items:
            "$ref": "#/components/schemas/BatchJobRequest"
          description: List of jobs to create (1-100)
        maxDepth:
          type: integer
          default: 2
          minimum: 1
          maximum: 10
          description: Maximum crawl depth
        maxPages:
          type: integer
          default: 200
          minimum: 1
          maximum: 10000
          description: Maximum pages to crawl per job
        headless:
          type: boolean
          default: false
          description: Use headless browser
        playwright:
          type: boolean
          default: false
          description: Use Playwright instead of Chromedp
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
          description: Request timeout in seconds
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
          description: Named auth profile to use
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        incremental:
          type: boolean
          default: false
          description: Enable incremental crawling
        sitemapURL:
          type: string
          format: uri
          description: Optional sitemap URL for seed URLs
        sitemapOnly:
          type: boolean
          default: false
          description: Only crawl sitemap URLs
        includePatterns:
          type: array
          items:
            type: string
          description: Optional URL path patterns to include during crawl discovery
        excludePatterns:
          type: array
          items:
            type: string
          description: Optional URL path patterns to exclude during crawl discovery
        respectRobotsTxt:
          type: boolean
          default: false
          description: Respect robots.txt rules during crawling
        skipDuplicates:
          type: boolean
          default: false
          description: Skip near-duplicate pages during crawling
        simHashThreshold:
          type: integer
          minimum: 0
          maximum: 64
          description: Hamming distance threshold for duplicate detection
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
    BatchResearchRequest:
      type: object
      description: Request to create multiple research jobs
      required:
      - jobs
      - query
      properties:
        jobs:
          type: array
          minItems: 1
          maxItems: 100
          items:
            "$ref": "#/components/schemas/BatchJobRequest"
          description: List of jobs to create (1-100)
        query:
          type: string
          description: Research query string
        maxDepth:
          type: integer
          default: 2
          minimum: 1
          maximum: 10
          description: Maximum research depth
        maxPages:
          type: integer
          default: 200
          minimum: 1
          maximum: 10000
          description: Maximum pages to research per job
        headless:
          type: boolean
          default: false
          description: Use headless browser
        playwright:
          type: boolean
          default: false
          description: Use Playwright instead of Chromedp
        timeoutSeconds:
          type: integer
          default: 30
          minimum: 5
          maximum: 300
          description: Request timeout in seconds
        authProfile:
          type: string
          pattern: "^[a-zA-Z0-9_-]+$"
          description: Named auth profile to use
        auth:
          "$ref": "#/components/schemas/AuthOptions"
        extract:
          "$ref": "#/components/schemas/ExtractOptions"
        pipeline:
          "$ref": "#/components/schemas/PipelineOptions"
        webhook:
          "$ref": "#/components/schemas/WebhookConfig"
        screenshot:
          "$ref": "#/components/schemas/ScreenshotConfig"
        device:
          "$ref": "#/components/schemas/DeviceEmulation"
        networkIntercept:
          "$ref": "#/components/schemas/NetworkInterceptConfig"
        agentic:
          "$ref": "#/components/schemas/ResearchAgenticConfig"
    BatchSummary:
      type: object
      description: Aggregate batch metadata and status.
      required:
      - id
      - kind
      - status
      - jobCount
      - stats
      - progress
      - createdAt
      - updatedAt
      properties:
        id:
          type: string
          description: Unique batch identifier
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
          description: Type of jobs in the batch
        status:
          type: string
          enum:
          - pending
          - processing
          - completed
          - failed
          - partial
          - canceled
          description: Current batch status
        jobCount:
          type: integer
          description: Number of jobs in the batch
        stats:
          "$ref": "#/components/schemas/BatchJobStats"
        progress:
          "$ref": "#/components/schemas/BatchProgress"
        createdAt:
          type: string
          format: date-time
          description: When the batch was created
        updatedAt:
          type: string
          format: date-time
          description: When the batch was last updated
    BatchProgress:
      type: object
      required:
      - completed
      - remaining
      - percent
      properties:
        completed:
          type: integer
          description: Number of terminal jobs in the batch.
        remaining:
          type: integer
          description: Number of jobs not yet terminal in the batch.
        percent:
          type: integer
          description: Completion percent based on terminal jobs.
    BatchResponse:
      type: object
      description: Stable batch envelope shared by create, get, and cancel responses.
      required:
      - batch
      - jobs
      - total
      - limit
      - offset
      properties:
        batch:
          "$ref": "#/components/schemas/BatchSummary"
        jobs:
          type: array
          items:
            "$ref": "#/components/schemas/Job"
          description: Included jobs for this response page.
        total:
          type: integer
          description: Total number of jobs in the batch.
        limit:
          type: integer
          description: Included job page size. Zero means jobs were not requested.
        offset:
          type: integer
          description: Included job page offset.
    BatchListResponse:
      type: object
      description: Paginated collection of aggregate batch summaries.
      required:
      - batches
      - total
      - limit
      - offset
      properties:
        batches:
          type: array
          items:
            "$ref": "#/components/schemas/BatchSummary"
        total:
          type: integer
          description: Total number of persisted batches.
        limit:
          type: integer
          description: Requested page size.
        offset:
          type: integer
          description: Requested page offset.
    BatchJobStats:
      type: object
      description: Aggregated job statistics for a batch
      required:
      - queued
      - running
      - succeeded
      - failed
      - canceled
      properties:
        queued:
          type: integer
          description: Number of queued jobs
        running:
          type: integer
          description: Number of running jobs
        succeeded:
          type: integer
          description: Number of succeeded jobs
        failed:
          type: integer
          description: Number of failed jobs
        canceled:
          type: integer
          description: Number of canceled jobs
    JobChain:
      type: object
      description: A named, reusable workflow definition
      required:
      - id
      - name
      - definition
      - createdAt
      - updatedAt
      properties:
        id:
          type: string
          description: Unique chain identifier
        name:
          type: string
          description: Human-readable chain name (unique)
        description:
          type: string
          description: Optional chain description
        definition:
          "$ref": "#/components/schemas/ChainDefinition"
        createdAt:
          type: string
          format: date-time
          description: When the chain was created
        updatedAt:
          type: string
          format: date-time
          description: When the chain was last updated
    ChainDefinition:
      type: object
      description: DAG structure defining the workflow
      required:
      - nodes
      - edges
      properties:
        nodes:
          type: array
          description: Job templates in the chain
          items:
            "$ref": "#/components/schemas/ChainNode"
        edges:
          type: array
          description: Dependency relationships between nodes
          items:
            "$ref": "#/components/schemas/ChainEdge"
    ChainNode:
      type: object
      description: A job template within a chain
      required:
      - id
      - kind
      - request
      properties:
        id:
          type: string
          description: Unique node identifier within the chain
        kind:
          type: string
          enum:
          - scrape
          - crawl
          - research
          description: Type of job
        request:
          type: object
          description: Operator-facing request payload matching the selected job kind
        metadata:
          "$ref": "#/components/schemas/ChainMetadata"
    ChainEdge:
      type: object
      description: A dependency relationship between nodes (From must complete before
        To can start)
      required:
      - from
      - to
      properties:
        from:
          type: string
          description: Source node ID (dependency)
        to:
          type: string
          description: Target node ID (dependent)
    ChainMetadata:
      type: object
      description: Human-readable metadata for a chain node
      properties:
        name:
          type: string
          description: Human-readable node name
        description:
          type: string
          description: Node description
    ChainListResponse:
      type: object
      description: List of job chains
      required:
      - chains
      properties:
        chains:
          type: array
          items:
            "$ref": "#/components/schemas/JobChain"
    ChainCreateRequest:
      type: object
      description: Request to create a new chain
      required:
      - name
      - definition
      properties:
        name:
          type: string
          description: Chain name (must be unique)
        description:
          type: string
          description: Optional chain description
        definition:
          "$ref": "#/components/schemas/ChainDefinition"
    ChainSubmitRequest:
      type: object
      description: Request to submit/instantiate a chain
      properties:
        overrides:
          type: object
          description: Operator-facing request overrides by node ID
          additionalProperties:
            type: object
    ChainSubmitResponse:
      type: object
      description: Response after submitting a chain
      required:
      - jobs
      properties:
        jobs:
          type: array
          items:
            "$ref": "#/components/schemas/Job"
    TransformPreviewRequest:
      type: object
      description: Request to preview a transformation on job results
      required:
      - jobId
      - expression
      - language
      properties:
        jobId:
          type: string
          description: Job ID to preview against
        expression:
          type: string
          description: JMESPath or JSONata expression
        language:
          type: string
          enum:
          - jmespath
          - jsonata
          description: Transformation language
        limit:
          type: integer
          default: 10
          minimum: 1
          maximum: 100
          description: Maximum number of results to transform
    TransformPreviewResponse:
      type: object
      description: Response containing transformed preview results
      properties:
        results:
          type: array
          items:
            type: object
          description: Transformed data items
        error:
          type: string
          description: Error message if transformation failed
        resultCount:
          type: integer
          description: Number of results returned
    TransformValidateRequest:
      type: object
      description: Request to validate a transformation expression
      required:
      - expression
      - language
      properties:
        expression:
          type: string
          description: JMESPath or JSONata expression to validate
        language:
          type: string
          enum:
          - jmespath
          - jsonata
          description: Transformation language
    TransformValidateResponse:
      type: object
      description: Response containing validation result
      properties:
        valid:
          type: boolean
          description: Whether the expression is valid
        error:
          type: string
          description: Error message if expression is invalid
        message:
          type: string
          description: Human-readable validation message
    WebhookDeliveryRecord:
      type: object
      description: A webhook delivery record tracking delivery attempts
      properties:
        id:
          type: string
          description: Unique delivery record identifier
        eventId:
          type: string
          description: ID of the event being delivered
        eventType:
          type: string
          description: Type of event (job.created, page.crawled, etc.)
        jobId:
          type: string
          description: ID of the job related to this event
        url:
          type: string
          description: Webhook URL that was called
        status:
          type: string
          enum:
          - pending
          - delivered
          - failed
          description: Current delivery status
        attempts:
          type: integer
          description: Number of delivery attempts made
        lastError:
          type: string
          description: Last error message if delivery failed
        createdAt:
          type: string
          format: date-time
          description: When the delivery record was created
        updatedAt:
          type: string
          format: date-time
          description: When the delivery record was last updated
        deliveredAt:
          type: string
          format: date-time
          description: When the delivery was successful (if applicable)
        responseCode:
          type: integer
          description: HTTP response code from the last attempt
    WebhookDeliveryListResponse:
      type: object
      description: Response containing a list of webhook delivery records
      properties:
        deliveries:
          type: array
          items:
            "$ref": "#/components/schemas/WebhookDeliveryRecord"
        total:
          type: integer
          description: Total number of delivery records
    DOMNode:
      type: object
      description: Simplified DOM node for visual selector builder
      properties:
        tag:
          type: string
          description: HTML tag name
        id:
          type: string
          description: Element ID attribute
        classes:
          type: array
          items:
            type: string
          description: CSS class names
        attributes:
          type: object
          additionalProperties:
            type: string
          description: Other HTML attributes
        text:
          type: string
          description: Text content (truncated)
        children:
          type: array
          items:
            "$ref": "#/components/schemas/DOMNode"
          description: Child elements
        path:
          type: string
          description: CSS path to this element
        depth:
          type: integer
          description: Depth in DOM tree
    DOMElement:
      type: object
      description: A matched DOM element in selector testing
      properties:
        tag:
          type: string
          description: HTML tag name
        text:
          type: string
          description: Text content (truncated)
        html:
          type: string
          description: Outer HTML (truncated)
        path:
          type: string
          description: CSS path to this element
    TemplatePreviewResponse:
      type: object
      description: Response from the template preview endpoint
      properties:
        url:
          type: string
          format: uri
          description: Fetched URL
        title:
          type: string
          description: Page title
        dom_tree:
          "$ref": "#/components/schemas/DOMNode"
          description: Simplified DOM tree
        fetch_time_ms:
          type: integer
          description: Time to fetch the page in milliseconds
        fetcher:
          type: string
          enum:
          - http
          - chromedp
          - playwright
          description: Fetcher engine used
    TestSelectorRequest:
      type: object
      description: Request to test a CSS selector
      required:
      - url
      - selector
      properties:
        url:
          type: string
          format: uri
          description: URL to test against
        selector:
          type: string
          description: CSS selector to test
        headless:
          type: boolean
          default: false
          description: Use headless browser
        playwright:
          type: boolean
          default: false
          description: Use Playwright instead of Chromedp
    TestSelectorResponse:
      type: object
      description: Result of selector testing
      properties:
        selector:
          type: string
          description: The tested selector
        matches:
          type: integer
          description: Number of matching elements
        elements:
          type: array
          items:
            "$ref": "#/components/schemas/DOMElement"
          description: Matching elements
        error:
          type: string
          description: Error message if selector is invalid
    TemplateDetail:
      type: object
      description: Full template details response
      properties:
        name:
          type: string
          description: Template name
        is_built_in:
          type: boolean
          description: Whether this is a built-in template
        template:
          "$ref": "#/components/schemas/Template"
          description: Template configuration
    Template:
      type: object
      description: Extraction template configuration
      properties:
        name:
          type: string
          description: Template name
        selectors:
          type: array
          items:
            "$ref": "#/components/schemas/SelectorRule"
          description: CSS selector rules
        jsonld:
          type: array
          items:
            "$ref": "#/components/schemas/JSONLDRule"
          description: JSON-LD extraction rules
        regex:
          type: array
          items:
            "$ref": "#/components/schemas/RegexRule"
          description: Regex extraction rules
        normalize:
          "$ref": "#/components/schemas/NormalizeSpec"
          description: Field normalization configuration
    SelectorRule:
      type: object
      description: CSS selector extraction rule
      properties:
        name:
          type: string
          description: Field name
        selector:
          type: string
          description: CSS selector
        attr:
          type: string
          description: Attribute to extract (or "text" for text content)
        all:
          type: boolean
          default: false
          description: Whether to extract all matches
        join:
          type: string
          description: Join string when extracting multiple values
        trim:
          type: boolean
          default: false
          description: Whether to trim whitespace
        required:
          type: boolean
          default: false
          description: Whether this field is required
    JSONLDRule:
      type: object
      description: JSON-LD extraction rule
      properties:
        name:
          type: string
          description: Field name
        type:
          type: string
          description: JSON-LD @type to match (e.g., "Article", "Product")
        path:
          type: string
          description: Dot-separated path in JSON-LD object (e.g., "author.name")
        all:
          type: boolean
          default: false
          description: Whether to extract all matches
        required:
          type: boolean
          default: false
          description: Whether this field is required
    RegexRule:
      type: object
      description: Regex extraction rule
      properties:
        name:
          type: string
          description: Field name
        pattern:
          type: string
          description: Regex pattern
        group:
          type: integer
          default: 0
          description: Capture group to extract
        all:
          type: boolean
          default: false
          description: Whether to extract all matches
        source:
          type: string
          enum:
          - text
          - html
          - url
          default: text
          description: Source to apply regex to
        required:
          type: boolean
          default: false
          description: Whether this field is required
    NormalizeSpec:
      type: object
      description: Field normalization configuration
      properties:
        titleField:
          type: string
          description: Field to use as title
        descriptionField:
          type: string
          description: Field to use as description
        textField:
          type: string
          description: Field to use as main text content
        metaFields:
          type: object
          additionalProperties:
            type: string
          description: Map of normalized keys to field names
    CreateTemplateRequest:
      type: object
      description: Request to create or update a template
      required:
      - name
      - selectors
      properties:
        name:
          type: string
          description: Template name (must be unique)
        selectors:
          type: array
          items:
            "$ref": "#/components/schemas/SelectorRule"
          description: CSS selector rules (at least one required)
        jsonld:
          type: array
          items:
            "$ref": "#/components/schemas/JSONLDRule"
          description: Optional JSON-LD extraction rules
        regex:
          type: array
          items:
            "$ref": "#/components/schemas/RegexRule"
          description: Optional regex extraction rules
        normalize:
          "$ref": "#/components/schemas/NormalizeSpec"
          description: Optional normalization configuration
