{
  "name": "spartan-scraper-extension",
  "version": "0.1.0",
  "description": "Browser extension for Spartan Scraper",
  "type": "module",
  "scripts": {
    "build": "tsc && pnpm run copy-assets",
    "copy-assets": "cp -r popup/*.html popup/*.css dist/popup/ && cp -r options/*.html options/*.css dist/options/ && cp manifest.json dist/ && cp -r icons dist/",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "pnpm run clean && pnpm run build && cd dist && zip -r ../spartan-scraper-extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "typescript": "^5.9.3"
  }
}
