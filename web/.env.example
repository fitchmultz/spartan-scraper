# Dev proxy target for `pnpm run dev` / `make web-dev`.
# Leave unset for the default local backend at http://localhost:8741.
DEV_API_PROXY_TARGET=

# Optional browser-visible API base URL for deployed cross-origin builds.
# Leave empty for local dev so requests stay same-origin through the Vite proxy.
VITE_API_BASE_URL=
