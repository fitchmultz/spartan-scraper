/**
 * Onboarding configuration constants shared across hooks/components.
 *
 * @module onboarding
 */

/**
 * Total number of steps in the onboarding joyride.
 * Keep this aligned with TOUR_STEPS in components/OnboardingFlow.
 */
export const ONBOARDING_TOTAL_STEPS = 8;
